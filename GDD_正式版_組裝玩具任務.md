# 遊戲設計文件（正式版）— 組裝玩具任務（暫定；開發代號 Assemble Toy）

> **產品名（暫定）**：組裝玩具任務 / Assemble Toy　**Tagline**：*"一個人，同時當駕駛、砲手、機械手。"*　（定名前查 Playdate Catalog 既有名稱與商標）
>
> 本文件為**正式版完整設計文件**：整合原型已驗證的核心機制 + 正式版的範圍與新增系統，可獨立閱讀。
> 原型版 [GDD](GDD_組裝玩具任務.md) 保留作「驗證歷史」；本文件為現行設計權威。
> 數值型參數一律**以 config（`parts_data` / `mission_data` 等）為準**，本文件只寫設計意圖。決策基準日：2026-07-28。

---

## 0. 定位與現況

- **原型軸心（已完成、已驗證）**：用 **crank + 方向鍵切換操作「自己組裝出來的機體」** → 手感成立、手忙腳亂但爽快（見原型 GDD §0、§4）。
- **正式版軸心**：把已驗證的核心，**無破綻地量產**成一款完整可發行的 Playdate 遊戲。
- **平台 / 商業（2026-07-28 拍板）**：**Playdate Catalog 單一平台、一次買斷**。crank 為核心機制，不移植到無 crank 平台。
- **最大風險（已從「機制做不做得出來」轉移）**：
  1. **美術總量**（你親手做）— ★最可能拖垮進度，尤其 5 隻 BOSS 與 25 關場景。
  2. **新零件功能的設計與調校**（不是程式難，而是每個新操作都要你試玩調手感）。
  3. Catalog 過審與上架行政。
- **現有已完成資產**（承接，不重做）：8 零件、6 關（ELIMINATE / DELIVER 兩型）、6 敵人型別、存檔、商店、HQ 組裝、結算、音效、斜坡整體傾斜、切換手感。**0 BOSS、0 多場景、0 場景互動物件**。

---

## 1. 一句話概念與目標感受

- **概念**：玩家用 crank 與方向鍵即時切換操控一台自組機器玩具，在橫向多場景關卡中手忙腳亂地完成各種任務。
- **目標感受**：手忙腳亂切換零件、緊張爽快（承接原型，不變）。正式版新增的過關條件與場景機關，都必須**服務於「逼玩家頻繁切換」**，而非稀釋它。
- **參考**：（原型仍空缺）方向為「一人分飾多職」。定名/宣傳期補。

---

## 1.5 劇情設定（2026-08-01 定案）

> 三項均已拍板,以下為定稿。實作前的舊文字(人類倖存者口吻)全部作廢。

### 世界觀
- **核戰後,人類已不存在。全世界只剩機器。**沒有任何人類角色登場(★同時是美術控量手段:不必畫人)
- **主角 = 一顆核心(Pilot)**,在廢墟中醒來,靠撿拾零件把自己的身體拼起來 → 直接對應組裝玩法
- **弱小機器 = 工作機器**(搬運/建築,完全無武裝),被獵殺、拆解成零件
- **壞機器 = 軍用機器**。★動機:**戰爭早就結束了,但沒有人告訴它們**——指揮系統最後一道作戰命令從未撤銷,它們至今仍在執行

### ★ 機器規格:軍規 vs 玩具規格(2026-08-03 定案)

> 這是**戰前的產業分類**,不是戰後才有的。世界觀不變,只是往上加一層分類。
> 本節是**造型與敘事的最上位依據**——所有美術與文案都以此為準,見 [ArtAssets](ArtAssets_切片_組裝玩具任務.md)。

| | **軍規** | **玩具規格** |
|---|---|---|
| 結構 | 焊死、一體成型 | 卡榫、模組化 |
| 改裝 | 不可能 | **本來就是設計來被改的** |
| 指揮鏈 | 在 → 接收命令 | 不在 → **從來沒有人對它下過令** |
| 誰 | 所有敵人、場景砲台、BOSS | **主角 + 工作機器** |

關鍵在最後兩列:**玩具的本質就是「買回來自己拼」**——可拆換不是缺陷,是設計目的;軍規為了單一任務最佳化,所以焊死。

#### 為什麼採用:它解釋了「已經做出來」的系統
本設定不新增任何機制,而是幫**現有系統補上理由**:

| 遊戲裡已經是這樣 | 理由 |
|---|---|
| 玩家能隨時換零件(HQ 3×2 格) | 玩具規格,本來就設計成可改裝 |
| 敵人造型固定、不能換零件 | 軍規焊死 |
| BOSS 被一塊塊拆掉、拆了就沒了 | 軍規的模組是**焊上去**的,拆下來裝不回去 |
| 工作機器完全無武裝 | 玩具規格沒有武裝掛點 |
| 玩家撿零件變強 | 玩具規格的擴充性 |
| 商店存在(全機器人世界誰在賣?) | 戰前的補給機,還在通電 |
| ★ **主角沒在執行那道未撤銷的命令** | ★ **不在指揮鏈上,從來沒收到過任何命令** |

#### 主題句
**你能贏,正是因為你不重要。** 軍方沒把玩具列入指揮鏈,因為玩具不是武器——那個「不被當一回事」就是你的自由。
最終戰可用:COMMAND CORE 能指揮全世界的機器,**唯獨無法對你下令**,因為它的系統裡沒有你的頻道。

#### crank = 發條
Playdate 的曲柄在物理上就是發條,而玩具是發條驅動的。主角機體應有**可見的發條孔/旋鈕**,把「操作介面」變成「幫一個玩具上發條」。

### 主題咬合
「拆解 vs 組裝」是同一個動詞的正反面:敵人**拆別人**變零件,你**拼自己**變強。BOSS 採組裝式、被一塊塊拆掉,正是「拆解者被拆解」。
**規格分類讓這組對比更鋒利**:軍規只能被**暴力拆解**(拆了裝不回去),玩具規格是**拼裝**(隨時可換)。同一件事,兩種手勢。

### 三幕
| 幕 | 內容 | 情緒 |
|---|---|---|
| 一 | 在廢墟中醒來,只是一顆核心。撿零件把自己拼起來 | 孤獨 → 站起來 |
| 二 | 遇見無武裝的工作機器,牠們被獵殺拆解。開始護送與反擊 | 有了要保護的東西 |
| 三 | 訊號源指向 **COMMAND CORE**——它還在等一個永遠不會來的命令。打倒它,戰爭才正式結束 | 敵人不是壞,是沒人喊停 |

規格分類讓三幕各多一層:**一**=你本來就沒有身體,玩具規格出廠就是等著被拼;**二**=工作機器與你同規格,是同類;**三**=COMMAND CORE 是軍規的最高點,最不能改變自己的東西。

### 敘事載體與規則
- 載體有三個:**開場 4 頁**([intro_data.lua](Source/intro_data.lua))、**每場景 dialog**(關卡 JSON 的 `scene.dialog`)、**結局 3 頁**([outro_data.lua](Source/outro_data.lua)),形式皆為「上方靜圖 + 下方打字機文字」
  - 開場第 1 頁為**核爆**(2026-08-03 加入),用途是先給毀滅的衝擊,再進「戰爭結束了」。畫法限制見 [ArtAssets §3](ArtAssets_切片_組裝玩具任務.md)
- **主角不說話**,全部用旁白/內心 → 省角色語氣設計,也符合「一顆核心」的孤獨感
- 文字規則:**每句需能單獨成立**(一次只顯示一句)、短句不用從屬子句,加上以下兩條:
  - **長度:量 px,不要只數字元**。文字框可用寬度 = **384px**(滿版 400 − 左右 padding 16;字寬在 `fonts/Assemble.fnt`,tracking=1)。
    40 個字元就可能超過而折行,折行會讓打字機節奏斷掉。量測指令見 [HANDOFF §5](HANDOFF_切片開發現況.md)。
    (原規則寫「≤ 40 字元」,2026-08-05 實測發現不準,已改為量 px。)
  - **句數:每頁最多 2 句**;**例外——開場最後一頁可 3 句**(2026-08-05 拍板)。
    理由:該頁需要「問題 → 回應 → 行動」三拍才推得動玩家進遊戲,砍第 3 句就只剩宣言沒有行動。
    ⚠️ 此例外**僅限開場最後一頁**;其餘各頁維持 2 句上限,因為 B 是**跳過整段**而非跳過一句,
    第 3 句開始玩家一按 B 就會連後面精心安排的內容一起錯過。
- **結局過場已實作**(S11,2026-08-03):載體增為三個,結局在 [outro_data.lua](Source/outro_data.lua)。全破 → 結算出現 `CONTINUE` → 播 3 頁結局 → CREDITS 標題顯示 **THE END**
- ★ **規格分類只用文字點兩次,其餘全部交給造型**——它的力量在於被「看出來」,講太多會變說明書。建議位置與句子:

| 位置 | 句子 | 長度 |
|---|---|---|
| 開場 | `They were built to follow orders.` | 33 |
| 開場 | `I was built to be taken apart.` | 30 |
| 最終戰前 | `It commands every machine alive.` | 32 |
| 最終戰前 | `It was never given a word for me.` | 33 |

---

## 2. 內容範圍（正式版 — 理想值 vs 垂直切片）

> solo 量產，數字先全部視為**理想值**，不當定案。**先做垂直切片量測單位成本**，再於「M2 規模拍板」回推是否維持或下修（見 §14、WBS）。

| 支柱 | 現有 | 理想值 | 垂直切片（先做） | 主成本落點 |
|------|------|--------|------------------|------------|
| 關卡 | 6 | **25** | **3～4 關**（涵蓋多場景+傳送、懸崖、場景武器、2 種新過關條件） | 你：場景美術、關卡擺放與試玩 |
| 零件 | 8 | **20**（+12，含新功能） | **+2～3**（至少 1 個「新功能」型、1 個純變體） | 你：零件圖；你：新功能調手感 |
| 敵人型別 | 6 | **+3（待規劃）** | **+1** | 你：敵人圖 |
| BOSS | 0 | **5（待規劃）** | **1** | ★你：BOSS 美術+多階段；設計狀態機（程式）|
| 過關條件 | 2 | **≥5**（+走到定點／保護 NPC／打倒 BOSS） | **走到定點 + 保護 NPC + 打倒 BOSS**（3 種） | 程式為主；保護 NPC 需試玩 |
| 場景系統 | 單場景 | 多場景+傳送、懸崖、場景武器、敵人重生點 | 全數在切片驗證 | 程式 + 你的場景圖 |

- **名稱決策【待拍板】**：「組裝玩具任務 / Assemble Toy」目前為開發代號。上架前查 Catalog 既有名稱與基本商標，有疑慮就改名並想 tagline。

---

## 3. 平台與操作

- **平台策略**：Playdate Catalog 單一發行。1-bit、400×240、30fps。輸入＝方向鍵 / A / B / crank（承接原型 InputSpec）。
  - **無多平台**：省下 PC/行動的第二套操作、觸控 UI、行動效能檔位、廣告 SDK、多商店送審。
  - Catalog 為**策展制、需過審**；打包為 `.pdx`，Panic 後台上傳。
- **操作對照**：完全承接原型 [InputSpec](InputSpec_組裝玩具任務.md)。正式版**新增**互動：
  - **場景武器物件**：機體靠近物件時出現提示，按 **A** 接管操作該場景武器（如固定砲台 / 開關 / 吊臂）；離開範圍或再按 B 解除。**接管期間的操作沿用「當前焦點」模型**（把場景武器視為臨時焦點），維持一致手感。
- **場景與鏡頭**：橫向側視、畫面捲動（承接）。**新增多場景**：一關由多個場景串接，經**傳送點**切換到下一場景（見 §9）。

---

## 4. 玩家狀態

- 機體 HP＝各零件 HP 加總，歸零＝失敗（承接，數值以 config 為準）。
- **新增失敗來源：墜崖**。場景可設**懸崖/落穴**，機體整體落到場景底線以下 → 立即關卡失敗（見 §9）。

---

## 5. 核心機制規格（已驗證 — 承接原型，不重述）

以下已於原型驗證成立，正式版**引用原型 GDD 為權威**，只在此標註「設計意圖」，不再展開規格：

- **兩層輸入（切換焦點 + 操作焦點）**：見原型 GDD §4。設計意圖＝切換本身就是手忙腳亂的來源，正式版所有新機關不得破壞「零延遲切換」。
- **機體整體感 / 斜坡整體傾斜**：見原型 GDD §5。設計意圖＝零件像鎖在同一台車上；新場景（含多場景、斜坡、機關）都沿用離屏整體傾斜方案。
- **組裝格 3×2 ↔ 操作介面對應**：見原型 GDD §6.2。正式版維持 6 格；**20 種零件是「可選池」擴大**，不是格數擴大。
- **槍口淨空 / 無操作零件不可選中**：見原型 GDD §6.3，維持。

> 原型的【待定】就地定案：以現行 `parts_data` / `mission_data` / 各 state 程式為準。

---

## 6. 敵人

- **基礎型（已實作 6 種）**：DRONE / SHIELD_ROBOT / BASIC_ENEMY / JUMP_ENEMY / SWORD_ENEMY / MINE（見 `entity_enemy.lua`、`mission_data` 敵人型別）。
- **正式版擴充法**：**基礎行為 × 數值/外觀變體**。
  - **變體（便宜）**：沿用既有行為、換數值/外觀 → 程式近乎免費，但**每種要一張圖**。
  - **新行為型別（+3，待規劃，較貴）**：需新 AI 邏輯（程式）+ 圖 + 試玩調難度。候選【待你選 3 種】：射擊後撤退型、空中投彈型、召喚型、護盾展開型、地形穿越型。
    → ⚠️ **2026-08-10 新增了另一組候選（隱形／爬牆／自爆／衝擊），見 §15.4。兩份清單需要合併或擇一，不要並存。**
- **新增：敵人重生點**：場景可設重生點，資料驅動（座標 + 型別 + 重生間隔/上限）。用於 ELIMINATE 之外的「撐時間」類條件；**回退**＝固定不重生（現行）。

---

## 7. BOSS（正式版全新系統）

- **架構**：每隻 BOSS 一套**行為狀態機**（多階段：依 HP 切換攻擊模式），資料/程式驅動；美術每隻一整套（idle / 多種攻擊 / 受擊 / 死亡 / 階段轉換）。
- **roster**：理想值 **5 隻（待規劃）**；切片先做 **1 隻**以量測「一隻 BOSS＝多少人日 + 多少張圖」。
- **成本提醒**：BOSS 是**桶①美術最大宗**與程式狀態機的主要工作量，5 隻很可能是全案最重的一塊；規模拍板時第一個檢視。
- **roster 候選**：巨大 BOSS（側面露上半身）、高速飛行 BOSS —— 見 §15.5。

---

## 8. 道具 / 經濟

- 承接原型：任務結算獲得資源 → 商店（`state_shop`）購買零件。正式版沿用，**擴充為 20 零件的購買池**與各關資源獎勵曲線（數值以 config 為準）。
- 零件**無升級、只更換**（承接原型設計原則）。
- **下一階段的零件構想**（上層多零件 + 7 種輔助型零件）見 **§15.1 / §15.2**。

### 8.05 核心（CORE）— 2026-08-01 定案

> 起因：劇情定案（§1.5）主角是「一顆核心」，但遊戲中原本沒有核心這個東西。核心補上這個缺口，
> 同時承擔兩件目前無人負責的事：**基礎 HP 的來源**、**讓 `weight` 從死數值變成實際限制**。

#### 設計原則

- **核心不佔組裝格。** 6 格已於 §5 拍板不變；且格子數＝玩家要同時顧的東西數量，
  吃掉一格等於直接稀釋「手忙腳亂切換」的核心體驗。核心是**被組裝的主體**，不是零件之一。
- **核心不在關卡中顯示**（藏在機體內部），關卡端零改動、零美術。
- **無升級、只更換**（沿用 §8 原則）：進程＝換上更好的核心，不是強化同一顆。

#### 規格

| id | 顯示名 | 基礎 HP | 負重上限 | 取得時機 |
|---|---|---|---|---|
| `CORE1` | `SALVAGE` | 30 | 16 | 開場預設持有 |
| `CORE2` | `FIELD` | 60 | 24 | 第二幕 |
| `CORE3` | `COMMAND` | 100 | 34 | 第三幕 |

- **HP 計算改為**：`核心基礎 HP + 已裝零件 hp 總和`。
  （這同時取代了 `save_manager.lua` 中來源不明的硬編預設值 `total_hp = 30`。）
- **負重**：`total_weight` 已由 `state_hq` 算出並顯示，但目前**沒有任何程式讀取它**。
  核心導入後，`total_weight > 核心負重上限` 時**擋在安裝階段**（不可裝並提示）。
- **不做「超重可裝但變慢」**：那需要接進移動邏輯、重調手感、重新驗證關卡；
  擋安裝是純 HQ 檢查，對關卡零風險。

> 負重上限的參考點（現有 8 零件實測重量）：`WHEEL1(4)+CLAW(8)+GUN(3)=15` 剛好過；
> `FEET(7)+CANON1(10)=17` 被 CORE1 擋下 ← 想用重腿就不能配最重的砲。

#### 8.05a 核心的取得與更換（2026-08-07 定案）

> **核心不是「選擇」，是「進度」。** CORE2 在每個維度都完勝 CORE1、CORE3 又完勝 CORE2，
> 所以拿到好的就永遠不會換回去——做選擇介面等於做一個答案永遠顯而易見的選單。
> §8「無升級、只更換」對**零件**成立（零件之間有重量/HP/功能的取捨），對**核心不成立**。

| 項目 | 決定 |
|---|---|
| **取得** | **關卡獎勵**。關卡 JSON 加 `"reward_core": "CORE2"`，與現有 `reward_steel/copper/rubber` 對稱 |
| **裝備** | **自動裝上**，沒有選擇介面。核心不佔格、不需擺放，多按一次沒有意義 |
| **降級** | 不會。`CoreData.grant()` 只在「比目前高階」時才換上，重打舊關卡不會把核心換回去 |
| **告知玩家** | 兩段：結算畫面把 `Mission Complete!` 換成黑底白字的 `CORE UPGRADED: FIELD`；按 A 後**插入專屬的核心升級畫面**（[state_core_upgrade.lua](Source/state_core_upgrade.lua)），看完才前往原本的目的地 |

**核心升級畫面（2026-08-07 加入）**
取得核心是整個進程中最重大的一次提升——**HP 上限、負重上限、跳躍能力三者同時跳一階**——
只在結算畫面閃一行字撐不起這個份量，所以給它一個專屬畫面。

```
        ■ CORE UPGRADED ■
          [核心圖 ×3 放大]
              FIELD
     HP        30 ->  60
     WEIGHT    16 ->  24
     JUMP    NONE -> MEDIUM   ← 有變化的值加框強調
            Press A
```

**跳躍用等級名稱、不用倍率數字**：`NONE` → `MEDIUM` → `HIGH`（`core_data` 的 `jump_label`）。
玩家不需要知道 ×1.3 是什麼意思，只需要知道「跳得更高了」。改倍率時記得一併確認等級名稱還說得通。

- 顯示**前後對照**，並在有變化的數值上加框——玩家一眼看到「解鎖了什麼」，尤其 `NONE -> x1.0` 那行是跳躍解鎖的唯一明示
- 目的地由結算畫面傳入（HQ / 選關 / 結局），本畫面只負責演出，不決定流向
- 核心圖尺寸自動讀取，日後換圖不必改版面

**為什麼不用商店買**：核心是劇情門檻。能用資源買代表玩家可能太早拿到（跳過設計好的「不能跳」段落），或錢不夠而卡關。
**為什麼不用幕別解鎖**：程式裡根本沒有「幕」的概念，進度只有 `completed_missions` + `prerequisite`，為此新增章節系統太重。

> ⬜ **仍待做**：HQ 目前只畫核心的圖，沒有顯示它的名稱與數值。DATA 框（97×49）已被 HP / WT 兩行佔滿，
> 第三行放不下（會超出框底 3px）。要顯示核心名稱得先決定放哪裡。

#### 8.06 跳躍歸屬（2026-08-07 定案）

> 起因：跳躍原本綁死在 FEET，導致**任何有懸崖的關卡都等於強制裝 FEET**，build 變化性被壓縮。

**跳躍 = 下層移動零件的共通能力 × 核心加成。**

| | 決定什麼 | 值 |
|---|---|---|
| 零件 `jump_height` | 基礎高度（px） | WHEEL1 30 / WHEEL2 42 / FEET 64 |
| 核心 `jump_mult` | 高度倍率 | CORE1 **0** / CORE2 1.0 / CORE3 1.3 |

實際高度 = 兩者相乘；初速度由程式反推（`v = sqrt(2gh)`），所以**調的是「跳多高」而不是「初速多少」**。

| | CORE1 | CORE2 | CORE3 |
|---|---|---|---|
| WHEEL1 | 不可跳 | 30 | 39 |
| WHEEL2 | 不可跳 | 42 | 55 |
| FEET | 不可跳 | 64 | 83 |

**設計理由**
- **不破壞 §1 的「逼玩家頻繁切換」**：跳躍仍綁在**焦點**上——焦點在 CANON 時按 A 是發射，要跳必須先切到移動零件。跳躍依然是要付出切換成本的戰術動作，只是不再限定 FEET。
- **CORE1 完全不能跳（倍率 0）**：第一幕「你只是一顆核心，連跳都跳不動」，取得 CORE2 是明確的能力解鎖，比單純加數值有感。
- **面板一致性**：跳躍鈕**只在能跳時顯示**（CORE1 階段整顆鈕不畫），所以不會出現「面板上有個按不動的鈕」。這也是核心唯一一次影響面板——透過「能不能用」，而不是自己佔一格。
> 現有零件理論最重組合約 20，故 CORE2 起對切片零件池幾乎全解禁；34 是留給正式版 20 零件池的重裝空間。

#### 【安全閥】

先將三顆核心的負重上限全部設為 `99`（等同無限制），把存檔／商店／HP 計算跑通後，
再收緊至上表數值。若試玩後判定負重限制不好玩，**把上限調大即等同關閉**，不需拆除程式。

#### 組裝畫面（`state_hq`）的顯示 — 三處

1. **DATA 框（狀態顯示・零美術成本）**
   框為 `x=7, y=95, w=97, h=49`，行高 16 → 正好 3 行，目前只用 2 行。新版：
   ```
   CORE: SALVAGE
   HP: 85
   WEIGHT: 24/32     ← 改為「目前/上限」，超重時反白警示
   ```
   框寬僅 97px，**核心顯示名必須 ≤ 8 字元**。

2. **機體正中央（存在感・可延後）**
   核心畫在組裝格正中央、**所有零件的底層**。16×16 原生像素（機體 2× 放大 → 螢幕上 32×32）。
   - 空 build ＝ 一顆孤零零的核心，**與 intro2 的畫面直接呼應**
   - 裝滿 ＝ 核心被自己拼出來的身體包住，只從縫隙露出
   - 已知代價：中間欄常被 2 格寬的 CLAW/CANON 蓋掉大半 —— 視為特色（拼裝把核心包起來），不修正

3. ~~**更換核心的入口 → 只在商店（`state_shop`）**~~
   **❌ 本項作廢（2026-08-12 使用者拍板）——「取得新核心時直接更換，不做任何更換介面」。**
   本項寫於 08-01，已被 08-07 的 §8.05a「自動裝上，沒有選擇介面」推翻；
   HANDOFF 長期記載的「⬜ 未做：核心更換介面」是承襲本項的舊描述，**不是待辦，是已否決**。
   > 連帶：`menu_items.lua` 的 `CORE(test)` 系統選單項**改列為永久開發工具**
   > （沒有它就只能洗存檔才測得到 CORE1 完全不能跳），不再是「等介面做好就拔掉」的暫時物。
   >
   > 原本記在這裡的版面事實仍然有效，已移入 §8.05b：
   > HQ 主選單 `MAIN_MENU` 現為 3 項，底中框亦僅容 3 筆（`LIST_VISIBLE = 3`），
   > 且主選單無捲動邏輯（`state_hq.lua:1396` 直接全畫）—— 加第 4 項會溢出框線。

#### 美術需求

- **3 張 16×16 的 1-bit 小圖**（`core1.png` / `core2.png` / `core3.png`），供機體中央與商店使用。
- `intro2.png` 的核心特寫**本來就要畫**，造型在此統一。

#### 8.05b HQ 版面重排 ＋ 組裝流程改版（✅ **2026-08-13 完成**）

> 立項起因：三個彼此無關的需求同時指向同一塊版面（核心名稱與數值、耐久顯示、零件變多）。
> 實作時使用者一併改了**操作流程**，所以這一節同時是版面規格與流程規格。

##### A. 流程改版（比版面更重要）

| 舊 | 新 |
|---|---|
| HQ 有 3×2 **操作面板** | **整段移除** —— 那是關卡中的東西，組裝畫面用不到 |
| HQ 有 **SHOP 方塊**進商店 | **移除**。改由 `TOP PARTS` / `BOTTOM PARTS` 進入 |
| 「商店」畫面 | 改名 **PARTS 介面**，同時負責 **購買 / 修理 / 安裝** |
| TOP/BOTTOM PARTS 在 HQ 內展開零件清單 | **改成進入 PARTS 介面**，並以 `GameState.parts_filter_row` 指定要顯示哪一排 |
| 核心畫在**組裝格正中央** | **移出格子**，改成右下角的**出擊按鈕**（核心即按鈕） |
| START 文字方塊 | 由核心圖取代 |

★ **零件的挑選、購買、修理全部集中在 PARTS**；HQ 只剩「機體長怎樣 + 這個零件放哪一格」。

##### B. 版面座標（量自新 `hq_bg.png`，工具：`tools/scan_hq_bg.py`）

| 區塊 | 座標 | 內容 |
|---|---|---|
| MISSION | x20 y19 **360×45** | 任務名（黑標籤）＋目標說明＋REWARD＋REQ |
| MENU | x31 y84 **135×50** | `TOP PARTS` / `BOTTOM PARTS` / `REMOVE PART`（行高 15，剛好 3 行） |
| DATA | x31 y167 **135×46** | HP / WT（框外底圖已有 DATA 標籤） |
| 組裝格 | `mech_cx/cy` 定位，96×64 | 16px × 2 倍放大 |
| 出擊按鈕 | `start_x/y`，64×64 | 核心圖 |

⚠️ `mech_cx/cy` 與 `start_x/y` **量不出來**（落在機艙裝飾圖上），是看畫面調出來的。
全部集中在 `state_hq.lua` 的 `HQ_LAYOUT` 一張表。

##### C. 出擊按鈕 ＝ 核心本體

圖：`core<N>-table-64-64.png`，格號規則：

| 格 | 用途 |
|---|---|
| 1 | 底座（常駐） |
| 2 | 未按 |
| 3 | 按下 |
| **4** | **選中（選配）** —— 有 4 格以上才用，否則退回第 2 格 |

- **底座與按鈕一律顯示**（離開焦點也看得到）；焦點**不畫外框**，由圖本身表現
- **按下時先播 8 幀動畫才進關卡** —— 舊版同一幀就 `setState`，那一格永遠來不及畫
- 換圖對應寫在 `core_data` 的 **`button_sprite`** 欄位（不是在程式裡拼字串）
- `core2` / `core3` 尚未繪製 → **載不到自動退回 `core1`**，流程照常

##### D. 其他

- **DATA 數值變動時 1.5× 放大再衰減**（安裝/拆卸/修理的回饋）
  ⚠️ Playdate **沒有** `drawTextScaled` —— 用 `imageWithText` + `drawScaled` 實作
- 左側「零件預覽框」移除（預覽移到 PARTS）；`NEED:` 提示改到 MISSION 框下方
- 選單焦點的黑底**閃爍**（250ms 節拍，與畫面其他閃爍元素同一拍）
- 安裝時**左側選單只顯示正在裝的那一個零件**（捲動視窗與 ▲▼ 已移除）
- 在不可安裝的格子按 A → **粗框快速閃爍（3 幀一換、白外黑內）＋ 錯誤音**

#### 8.05c PARTS 介面規格（✅ 2026-08-13）

版面量自 `shop_bg.png`（全部 100% 乾淨矩形）：

| 區塊 | 座標 | 內容 |
|---|---|---|
| 資源列 | x6 y6 386×21 | STEEL / COPPER / RUBBER |
| 清單 | x6 y36 121×192 | **只顯示名稱**（行高 20 → 9 行） |
| 預覽 | x137 y36 255×117 | 名稱＋圖＋（未擁有：購買資源／已擁有：耐久度＋修理資源） |
| 說明 | x137 y162 166×66 | `description` |
| 按鈕 ×3 | x313 y162/186/210，各 79×18 | `BUY`\|`REPAIR` ／ `INSTALL` ／ `BACK` |

**操作模型**：游標在「清單」或「三顆按鈕」之間
- 清單：上下選零件、**CRANK 捲動**（30°/格）、右鍵或 A 跳到按鈕、B 回 HQ
- 按鈕：上下換鈕、左鍵回清單、A 執行、B 回 HQ

**規則**
- 顯示該排**全部零件**（含未擁有）—— 購買與組裝同一個畫面，只需要一套清單
- **不可用的按鈕整顆隱藏**（不是淡化）。★ 游標必須跳過隱藏的鈕，否則會「選著一顆看不見的鈕」；
  可用判斷抽成 `buttonEnabled()`，**繪製端與輸入端共用**
- `INSTALL` → 設 `GameState.pending_install` 並回 HQ；HQ 於 setup 進入「選位置」狀態，
  沿用既有的 `selected_category` + `selected_part_index` 機制（游標粗框、左右移欄、按 A 安裝全是現成的）
- 裝完**留在 HQ**（可直接看整台機體、接著出擊）

### 8.07 零件耐久 / 損壞（⏸ 2026-08-08 討論後暫緩 → ★ **2026-08-12 重啟，本階段執行**）

> **目的已釐清:讓商店與資源產生取捨壓力**（不是為了增加戰鬥變數）。
> 討論結論記在這裡,之後要做直接照這份走,不用重新想。

**★ 因為目的是經濟而非戰鬥，所以不用碰傷害模型。**
共享血量池（`current_hp` = 核心 base_hp + 零件 hp 總和）**維持現狀**，
不需要定位傷害、不需要零件在關卡中個別損壞 —— 全部只發生在關卡外。

**已達成的設計共識**

| 項目 | 決定 |
|---|---|
| 耗損時機 | **只在過關時結算**。★失敗不扣耐久——否則玩得差的人「又輸又花錢」，只會加速卡關；重試永遠免費，推進才有成本 |
| 耗損量 | 依**這場掉血的比例**扣，不是固定值。玩得好＝少耗損，讓技術直接換算成錢 |
| 耐久歸零 | **不能裝備，但零件還在**，修好即可用。不做「效能下降」（會把戰鬥數值又拉進來）、不做「零件消失」（玩家會囤著不敢用） |
| ★ 修理費 | **與零件成本掛鉤 → 高階零件維護成本高**。這是免費的副產品，但它同時補上「組裝決策沒重量」的缺口——不再是無腦裝最強 |
| 介面位置 | **商店加一個 REPAIR 分頁**（已有資源列與確認框，不必做新畫面） |

**★ 這個設計最大的風險**
> 若資源永遠夠用，修理就只是每關後按一下的手續，沒有任何決策。

要成立，玩家在修理畫面前必須猶豫這三件事:**修哪一個 / 修還是買新的 / 帶半殘硬上還是花光資源**。
也就是**修理費必須與關卡獎勵在同一個量級** —— 這只能靠實際玩測抓，屬於 QA 項目，無法先算。

#### ★ 2026-08-12 重啟時的三項拍板

| 題目 | 拍板 | 代價（已告知並接受） |
|---|---|---|
| **耗損曲線** | **打得好幾乎不耗** —— 掉血 0% 不扣，掉血愈多才扣愈多 | 熟練玩家可能幾乎感受不到這個系統，REPAIR 分頁形同虛設 |
| **修理費量級** | **修理費 = 缺的耐久比例 × 零件成本 × 小係數**，把絕對值壓到與關卡獎勵同量級；**獎勵曲線不動** | 保留「高階零件維護貴」，但取消原本一次修 FEET 要花十幾關收入的極端值 |
| **耐久顯示** | **整個組裝介面重新安排** → 見 §8.05b | 版面成本比「壞掉才標記」高，但一次解決三個需求 |

> 量級依據（2026-08-12 實測）：零件總成本 **25～265**（GUN 25 / FEET 265），
> 而關卡獎勵目前只有 **12～65**。兩者差一個數量級，故從修理費那側解。

#### ★ 一個對設計有利的現況（2026-08-12 查證）

存檔是 `owned_parts[part_id] = true` —— **型別擁有制，同一種零件只會有一把**。
所以耐久就是 `part_durability[part_id] = n`，**不必處理多實例**。
副作用是壓力比預期更強：**FEET 壞了就真的沒腿可用**，沒有備品可換。

**尚未決定**:耗損公式與修理費的具體算法（形式與數值都還沒定）。
要繼續時的第一步就是定這個，因為它決定存檔要存什麼、UI 要顯示什麼。

**已知的實作成本**:存檔新增 per-part 耐久欄位、結算時計算耗損、商店 REPAIR 分頁、
HQ 擋下損壞零件的安裝（比照負重上限，已有先例）、零件清單顯示耐久（**UI 空間吃緊，
建議「壞掉才標記」而非隨時顯示數字**）。

### ★ 8.08 資源經濟：獲得方式（2026-08-12 拍板）

> 起因：使用者主動提出「打倒敵人是否掉資源」「重打同一關是否還給獎勵」。
> 查證後發現這不是新功能，**是在補一個已經破掉的洞**。

#### 現況（2026-08-12 查證）

[state_result.lua:113-130](Source/state_result.lua:113) **無條件給獎**，
`completed_missions[id] = true` **只用來標記進度、不影響給獎**。
→ **現在可以無限重刷同一關賺資源。**

這正是 §8.07 點名的最大風險：*「若資源永遠夠用，修理就只是每關後按一下的手續，沒有任何決策。」*
**耐久系統要成立，這個洞必須先補。**

#### 兩個構想剛好互相解決對方的問題

| 構想 | 解決什麼 | 單獨做的問題 |
|---|---|---|
| **首過關限定獎勵** | 製造稀缺 | ⚠️ 資源全部有限 → 打得爛的玩家用光資源、零件又壞光 → **永久卡關** |
| **打敵人掉落** | 可重複的保底收入 | 單獨做＝換個方式無限刷，稀缺性依然消失 |

**兩個一起做，各自的問題就消失**：首過關給大獎（推進動力）＋ 打敵人給零頭（保底，可刷但效率低到不值得刷）。

#### ★ 與耐久接成一個閉環

> 打敵人才有錢 → 但打敵人會掉血 → 掉血耗耐久 → 耐久要花錢修

**打得好：賺得多、花得少。打得爛：賺得少、花得多。**
比單獨的耐久更能達成 §8.07 想要的「技術直接換算成錢」。

#### 拍板

| 題目 | 決定 |
|---|---|
| **重打已過關的關卡** | **完全不給關卡獎勵**。首過關才有。選關畫面已有 `CLEAR` 標記，玩家一眼知道哪關還有獎勵 |
| **掉落收取方式** | **掉在地上、走過去碰到才收**。創造「停下來撿 vs 繼續推進」的取捨 |
| **掉落內容** | **種類依敵人型別固定**（寫在 `enemy_data`），**數量在小範圍內隨機** |

#### ★ 意外收穫：反向槍終於有理由了

掉落物落在地上、玩家一路往右推進 —— **不回頭就撿不到**。
這正好給 §15.9 第 3 項「配合反向槍的場景設計」一個天然理由：
**折返撿資源的關卡結構**，而折返時背後就會有敵人。

#### ⚠️⚠️ 必須有的安全閥：死鎖

**「耐久 0 擋出擊」＋「重打不給獎勵」＝ 真的可能永久卡關**：
零件全壞 → 不能出擊 → 賺不到資源 → 修不好 → 永遠不能出擊。

初始存檔是 `owned_parts = { GUN, WHEEL1 }`、`resources = 0`（[save_manager.lua:57-68](Source/save_manager.lua:57)），
所以破產狀態是真的到得了的。

**★ 拍板（2026-08-12）：初始零件 `GUN` / `WHEEL1` 不吃耐久、永不損壞。**
- 玩家永遠組得出一台能出擊的機體 → 永遠能靠掉落賺錢 → 死鎖消失
- 兩者都是最便宜的零件（各 25），不會破壞「高階零件維護貴」的取捨
- 敘事上也說得通：撿來的破爛零件本來就已經是最爛狀態

**實作方式**：`parts_data` 加一個 `indestructible = true` 欄位（GUN / WHEEL1），
結算耗損時跳過。**用資料欄位而不是寫死 id 清單** —— 日後若改由別的零件當保底，改資料即可。
⚠️ 商店的 REPAIR 分頁也要跟著跳過這些零件，否則會出現「修理費 0」的空選項。

#### 掉落對照表（2026-08-12 拍板）

原則：**型別固定 ＋ 主題對應 ＋ 強度分級**。數量在小範圍內隨機。

| 敵人 | hp | 掉落 | 數量 | 主題理由 |
|---|---|---|---|---|
| DRONE | 10 | 銅 | 1–2 | 電子類 |
| SHIELD_ROBOT | 10 | 鋼 | 1–2 | 裝甲類 |
| JUMP_ENEMY | 15 | 橡膠 | 1–2 | 彈跳＝橡膠 |
| WALKER_ENEMY | 18 | 銅 | 1–2 | 射擊類 |
| BASIC_ENEMY | 20 | 銅 | 1–2 | 射擊類 |
| SWORD_ENEMY | 30 | 鋼 | 2–3 | 刀刃＝鋼 |
| **HEAVY_ENEMY** | **80** | 鋼 | **4–6** | 最硬＝最值錢 |
| **BOSS** | — | 三種 | 各 5–8 | **只掉一點**——BOSS 的報酬主要來自首過關獎勵與核心，不讓重打變成最佳刷法 |
| MINE | 1 | **不掉** | — | 是陷阱不是敵人，掉落＝免費資源 |

> 粗估：M003 有 13 隻敵人，全清約 **26 資源**。
> 對照修理費 3～80、零件成本 25～265 —— 當保底剛好，刷起來很慢，不會蓋過首過關獎勵。

#### 其餘拍板

| 題目 | 決定 | 理由 |
|---|---|---|
| **掉落物會不會消失** | **不消失** | 不會因為漏撿而挫折；「回頭去撿」的取捨仍在（時間成本、背後的敵人），限時關卡本來就有壓力 |
| **REACH 類關卡收入趨近 0** | **可以接受，這就是取捨** | 趕時間衝過去 vs 停下來清場賺資源，本來就是玩家要做的決定。不另給保底 |
| **過關寬限**（見下） | **過關判定成立後留 3 秒再結束** | 完整保留「走過去撿」的取捨，也不會有白白損失的挫折感 |

#### ⚠️ 過關寬限：為什麼非做不可

掉落物是在敵人 `is_alive` 轉 false 的那一刻生成的，
而 `ELIMINATE_ALL` / `BOSS_KILL` 的過關判定（[state_mission.lua:867](Source/state_mission.lua:867)）
**也是**等最後一隻敵人爆完 —— **兩件事發生在同一瞬間**。

→ 沒有寬限的話，**最後一擊的掉落永遠來不及撿**。這不是玩家技術問題，是規則本身的漏洞。

**實作**：`CLEAR_GRACE_TIME = 3.0` 秒，寫在 `completeCurrentScene()` **裡面**。
- ★ 過關的呼叫點有 **6 處**（傳送點／ELIMINATE_ALL／REACH／PROTECT／DELIVER…），
  逐處加寬限一定會漏（HANDOFF §3-5）→ **集中在這一個出口**
- 場上沒有掉落物時**完全不延遲**，既有手感不變
- 撿完了不必等滿 —— 下一幀 `#drops == 0` 就直接過關

#### 美術需求

| 檔名 | 尺寸 | 內容 |
|---|---|---|
| `drop-table-8-8.png` | **8×8 × 3 格** | 掉落物小圖：鋼 / 銅 / 橡膠各一格 |

⚠️ 檔名**必須**是 `-table-寬-高` 格式（HANDOFF §3-2，本專案最常踩的坑）。
⚠️ 8×8 在 1-bit 下極小，三種資源要**用形狀區分**（不能靠灰階或紋理），且落在黑色地面上要看得見（§3-4）。

### 8.1 進程結構與升級系統（設計方向・待手感驗證）

> 未定案，先不寫進主規格。列為決策記錄，交給切片試玩回答。

- **已定案的部分**：進程主軸＝核心三階（見 §8.05）。核心不佔格、不影響操作，
  純數值成長，是最適合當進程軸的東西 —— 玩家立刻感覺變強，卻不新增操作負擔。
- 待決：是否需要「零件解鎖進程」（打特定關才解鎖某零件）？還是純用資源購買即可？
- 待決：25 關的推進是**線性解鎖**還是**分支選關**？
- **回退**：維持原型的「完成前置關 → 解鎖下一關」線性 + 商店自由購買。

---

## 9. 勝負 / 結束 / 關卡進程

- **過關條件（理想值 ≥5）**：
  1. ELIMINATE_ALL（已實作）
  2. DELIVER（已實作）
  3. **走到定點 REACH**（新；最便宜，程式為主）
  4. **保護 NPC 一段時間 PROTECT**（新；需 NPC 移動/存活判定 + 撐時間，配合敵人重生點；**待驗證**是否好玩、**回退**＝改為限時 ELIMINATE）
  5. **打倒 BOSS BOSS_KILL**（新；依賴 §7）
- **失敗條件**：機體 HP 歸零 / 限時到未達標（承接）＋ **墜崖**（新，§4）。
- **多場景關卡進程（新）**：
  - 一關 = 場景序列 `[場景1] →(傳送點)→ [場景2] → …`。到達傳送點且滿足該場景條件 → 載入下一場景；最後一個場景達標 → 通關結算。
  - **待驗證**：多場景的載入/鏡頭切換順不順、記憶體是否吃得住（1-bit 資產小，風險中低）。**回退**＝單場景長關（現行 mission_data 已支援長場景 + 捲動）。
- **場景互動物件（新）**：懸崖（§4）、場景武器（§3）、傳送點、敵人重生點（§6）、REACH 定點、PROTECT 的 NPC — 全部設計為 **`mission_data` 資料驅動**，沿用原型「關卡＝資料表」的量產優勢（可用你的網頁版關卡編輯器擴充擺放）。

---

## 10. 正式版新增系統（程式 / AI 可代勞）

> 這些是純程式，AI coding agent 可代勞——**不是瓶頸**，列出讓你安心。

- **多場景載入與傳送**、**墜崖判定**、**場景武器接管框架**、**敵人重生器**、**三種新過關條件的判定**、**BOSS 狀態機框架**。
- 前端/進程補完：選關流程整合、關卡進程存檔擴充（承接現有存檔）、暫停選單、設定選單（音量/操作說明）。
- **劇情/過場（確定要做）**：
  - **開場過場**：新遊戲開始播一段 **2-3 張圖 + 文字**，形式**同關卡開始前的劇情畫面**（沿用現有 dialog 過場框架：上方靜圖 + 下方打字機文字、可跳過）。
  - **關前劇情**：承接原型現有的關前對話（`mission_data.dialog`，共用底圖 + 文字，已存在）。
- **操作教學（新遊戲，確定要做）**：首次遊玩的簡易教學，涵蓋 **① 組裝介面 ② 商店 ③ 關卡內操作**。以現有 dialog/提示框呈現（文字為主、少量提示圖），首次觸發、可跳過，完成狀態存檔避免重複。

---

## 11. 商業模式

- **一次買斷、Playdate Catalog**。無廣告、無內購 → 不需廣告 SDK、不需第三方分析、不需 GDPR/隱私同意流程（大幅省行政與法務）。
- 上架前置：Catalog 送審素材（封面 art、截圖、說明）、定價、名稱/商標確認。
- **跨平台**：🔒 **上架 Catalog 之後**才評估，結論已封存在 **§16**（速讀：技術可行，但**若要離開 Playdate，第一站應是 PC 而非手機**）。

---

## 12. 美術 / 音訊 / 外部資源（人工；非程式）★本案真正瓶頸

> 明細與量化見 [WBS 正式版](WBS_正式版_組裝玩具任務.md) D 區。此處只列重點旗標：

- **桶① 你親手做（1-bit）**：**BOSS 5 套（最重）**、12 個新零件圖（每個 base + 操作面板，部分含子件/動畫）、+3 敵人圖、25 關場景/機關/傳送/懸崖/場景武器/NPC 美術、UI（選關、HUD、標題、Catalog 封面）、**開場過場 2-3 張圖 + 教學少量提示圖**。**關前劇情沿用共用底圖 + 文字（已存在，不畫每關插圖）** → 劇情美術總量壓在低位。
- **桶② 外部授權**：Playdate 用**系統字型**、英文市場 → **CJK 付費字型問題消失**；BGM/音效若沿用現有需確認商用出處，新增則找**商用+可散布**素材。
- **桶③ 帳號/行政**：Playdate 開發帳號、Catalog 送審、**名稱商標查詢**。**無** Steam $100、**無** Apple/Google、**無**廣告隱私政策。
  - ⚠️ 這一行「無」的前提是**只上 Playdate**。§16 的跨平台一旦啟動，Steam $100／Apple US$99 年費／Google US$25、隱私政策、分級、各裝置尺寸截圖**全部會回來** —— 屆時本節要重算。
- **結論**：程式不是瓶頸；**你的美術總量（尤其 5 BOSS + 25 關場景）與新零件手感調校才是**。這是規模拍板要正視的核心。

---

## 13. 技術與風險

- **引擎/工具**：Playdate SDK（Lua）、pdc、AI coding agent 輔助。承接原型的離屏傾斜繪製、資料驅動關卡。
- **正式版新增風險**：
  1. **美術量產速度**（桶①）— 用世界觀分組共用素材、變體優先，控管總量。
  2. **BOSS 狀態機 + 美術**（§7）— 切片先做 1 隻量成本。
  3. **多場景載入/鏡頭/記憶體**（§9）— 1-bit 資產小，風險中低；切片驗證。
  4. **新零件功能調手感**（§2）— 每個新操作都要你試玩，落在你身上。
  5. Catalog 過審不確定性 — 早讀送審規範。

---

## 14. 開發順序

**垂直切片 → 量測單位成本 → 拍板規模 → 量產 → 上架**（詳見 WBS 依賴主線與時程）：

1. **切片**：新系統骨幹（多場景/傳送/墜崖/場景武器/重生點/3 新條件/BOSS 狀態機）+ 前端進程 + **3～4 關 + 1 BOSS + 少量新零件/敵人** → 達正式版品質的可玩全流程。
2. **量測**：一關、一隻 BOSS、一個新零件各要多少人日與多少張圖。
3. **拍板**：用切片成本回推 25 關 / 5 王 / 20 零件是否維持或下修。
4. **量產 → 上架**。
5. 🔒 **（上架之後才啟動）** 跨平台移植評估 —— 見 **§16**。**在 4 完成前不做任何移植工作，也不為了移植而改變現在的架構。**

> ⚠️ **下一階段的構想已登記在 §15**，其中有數項會**改動已驗證的手感**（GUN 改手動、CLAW 攻擊力改制），
> 依 §14 的邏輯，那些應該排在「量測」之前或緊接其後 —— 因為它們會讓已量測到的單位成本失效。

---

## 15. 下一階段構想池（2026-08-10 登記）

> **這是想法登記，不是排程、也不是定案。** 依本專案慣例（先討論設計、再實作），
> 每一項在動手前都應該先確認方向；這裡只保證「想到的東西不會忘掉」。
>
> 標記說明：**【結構】**＝會動到既有系統的骨架 ‧ **【新增】**＝加東西、不動舊的 ‧
> **【改制】**＝會改變已驗證的手感（風險最高） ‧ 每項附「程式 / 美術」的粗略落點。
> 美術成本一律當作**瓶頸**看待（§12），所以「要幾張圖」比「要幾天程式」更值得先看。

### 15.1 ~~【結構】~~【新增】上層可裝多個零件

> ## ★★ 2026-08-12 查證：**這一項在程式上已經成立了，不必實作。**
>
> [state_hq.lua:135-179](Source/state_hq.lua:135) 的 `checkIfFits()` 只檢查四件事：
> 超出邊界、`placement_row` 對不對排、**格子有沒有被佔用**、槍口淨空。
> **沒有任何「上排只能一個零件」的規則。**
>
> 玩起來像是只能裝一個，原因是**零件寬度**：上排只有 3 格，而 10 個零件裡**只有 GUN 是 1 格寬**
> （GUN2/CLAW/CANON1-3/SWORD 全是 2 格，WHEEL1-2/FEET 是 3 格且屬下排）。
> 所以現階段唯一裝得下的雙零件組合是 `CANON(2格) + GUN(1格)` —— 而**這個組合現在就能裝**
> （`requires_clear_right` 會逼 GUN 放最右邊，剛好合理）。
>
> **→ 本項因此從【結構】降級為【新增】。真正的前提不是「能不能裝兩個」，而是「有沒有 1 格寬的零件可裝」。**
>
> 連帶失效的規劃：§15.7 把本項排在第 1 位、理由是「最貴最基礎」—— **不再成立，可以直接跳到做零件**。
>
> 焦點切換壓力的**物理上限**也因此明確了：上排 3 格 → 最多同時存在 **3 個 1 格寬的 `operable` 零件**。

~~目前上層一次只有一個功能零件。開放成多個之後，下面 15.2 那些輔助型零件才有意義
——這是 §15 大半項目的共同前提，要做就得先做它。~~

**★ 這一項的真正成本不在「能不能裝兩個」，而在焦點切換。**
現況已經是「跳躍綁在焦點上」（見 §8.06、[HANDOFF §2-2](HANDOFF_切片開發現況.md)），焦點在 CANON 時 A 是發射不是跳。
上層零件從 1 個變成 N 個，焦點循環就從 2~3 項變成 4~5 項，**切換壓力會等比放大**。
可能的緩解方向（未決）：輔助型零件一律 `operable = false`（不進焦點循環，被動生效），
只有主動武器才佔焦點。若採這個原則，15.2 的多數項目都會落在「被動」那一側。

| 也要一起想的 | 說明 |
|---|---|
| 負重上限 | 核心 `weight_cap`（16/24/34）就是天然的節流閥,不必另做限制 —— 但要重新確認上限值 |
| 版面 | HQ 組裝格與關卡操作面板要放得下 N 個零件的 UI |
| `requires_clear_right` | GUN 現有的「槍口淨空」規則,在多零件下會更常擋住安裝 |

⚠️ 這一項若成立，**§16.3 的跨平台結論要重寫** —— 零件愈多，觸控版愈難不走向「一顆零件一顆按鈕」，而那會讓焦點切換的張力消失。

### 15.2 【新增】輔助型上層零件

| 零件 | 內容 | 依賴 | 落點 |
|---|---|---|---|
| **防護罩** | 擋傷害 | — | ✅ **2026-08-13 完成**。1格、被動、`weight 4`（吃負重）。擋一次 → 冷卻 5 秒 → 恢復。<br>★ 攔在**傷害套用之前**，不碰傷害模型。<br>⚠️ 虛線圓目前**只是視覺** —— 傷害由 controller 加總後才回傳，分不出方位，實際是「擋下一次任何傷害」。要真的按範圍擋需改成逐來源判斷。 |
| **自動發射** | **【改制】現有 GUN 先改成手動**，裝了這個零件才有自動 | 15.1 | ⚠️ 見下方警告 |
| **吊索鉤** | 掛上吊索、離開地面 | — | ✅ **2026-08-13 完成**。**2格**、`operable`。A=往上發射鉤子／左右=沿索移動／**crank=捲索長度**。<br>★ 焦點切走**仍維持掛著**；放開條件只有「再按 A」或「移動到索道邊緣」。<br>★ 放索**不能穿過地面**（夾 `hook_len` 本身，不是只夾 y）；pit 上方不夾。 |
| **偵測器** | 讓隱形敵人隨時可打 | — | ✅ **2026-08-13 完成**（與隱形敵人同批）。1格、被動、`weight 3`。<br>★ 旗標每幀在 controller 算**一次**，不讓每隻敵人自己掃裝備清單。 |
| **高位槍** | 拋射、攻擊力較弱 | — | ✅ **2026-08-19 定案：改成拋射武器**。1格、`operable`（手動 A）、`projectile_grav_mult = 18` 打明顯弧線。<br>★ 因為是弧線，可越過前方敵人／障礙打到後面 —— **「高」不再靠圖的高度，也不再需要特定高度的敵人**。<br>★ 拿掉槍口淨空 → 可與 GUN/CANON 並排；代價是子彈可能被自己的武器擋掉（`self_block`，刻意取捨）。<br>⚠️ `gun_high.png` 仍是 `gun.png` 複本，只差**外觀辨識度**（尺寸維持 24×16）。 |
| **反向槍** | 往**左**射擊 | — | ✅ 程式完成。1格、被動。`fire_direction`＋`requires_clear_left`（鏡像的槍口淨空）。<br>⚠️ **場合尚未建立** —— 見 §15.9 第 3 項。 |
| **追蹤飛彈** | 先往上射再轉向追敵 | — | ✅ **2026-08-13 完成**。**2格**、`operable`（手動 A）。<br>★ 閥門是 `missile_turn_rate`(120)：轉太快＝必中不用瞄、太慢＝繞圈打不到,**只能試玩定**。 |

> ⚠️ **「GUN 改手動」是本節唯一會動到已驗證手感的一項。**
> GUN 目前是 `operable = false`（全自動、不進焦點循環）。改成手動＝**它會進入焦點循環**，
> 直接加重 15.1 已經點出的切換壓力，而且會改變所有既有關卡的難度基準。
> **建議：這一項單獨拉出來先試玩驗證，不要跟其他零件綁在同一批做。**
> 若試玩結果是「手動槍不好玩」，退路是**維持 GUN 自動、改成用另一個零件提供更強的自動火力**。

### 15.3 【新增】場景物件

| 物件 | 內容 | 落點 |
|---|---|---|
| **吊索** | 上方索道 | ✅ **2026-08-13 完成**。`scene.ropes=[{x1,x2,y}]`，**只做水平**（斜的會讓沿索移動變成二維問題）。目前程式繪製。 |
| **可破壞石塊** | 打得掉的障礙 | ✅ **2026-08-13 完成**。obstacle 加 `"hp": N` 就可破壞（沿用既有 obstacle 系統,它本來就擋移動）。<br>⚠️ 障礙物的 y 由遊戲用 `ground_y - height` 算，**JSON 的 y 不會被讀**。 |
| **側面牆面** | **把目前只是背景的那一層（雲等）變成可站立的牆面**，供爬牆敵人使用 | ⚠️ 見下方 |
| **移動地板** | 平台遊戲常見 | ⚠️ **2026-08-13 拍板：延後、單獨做**。<br>★ 它**不符合現有地形模型** —— 地形是「固定 64px 一格的靜態陣列，用世界 x 查表」(`getGroundHeight`)。<br>會動的平台不能靠 x 查表,得做成**獨立實體**（自己的碰撞、自己的高度判定），<br>還要處理「站在上面的角色跟著位移」（玩家／敵人／掉落物都會踩到）。<br>★ 刻意**不**硬塞進 terrain 陣列 —— 那會變成「地形有時候會動」的特例,<br>之後每個讀 `terrain` 的地方都要記得它。 |
| **鬆動地板** | 踩上去一段時間會掉落 | ✅ **2026-08-13 完成**。地形型別 `crumble`：踩 0.8 秒後塌陷。<br>★ 塌陷後**沿用 pit 的「回傳極大值」** → 掉落判定/機體物理/敵人踩踏全部自動沿用,沒有第二套。<br>★ 預警＝表面層的 **dither 濃度閃爍**（1-bit 上唯一能表現「不穩」又不糊掉的方式）。<br>★ `getTerrainPoints` 對未知型別回傳水平線 → 未塌時自動畫得跟 flat 一樣,不必加繪製分支。 |

> ⚠️ **側面牆面要留意**：`backgrounds`（畫一次、視差 0.3/0.6）與 `sky`（橫向平鋪、視差 0.1）
> 目前都是**純裝飾、不參與碰撞**（HANDOFF §2-2）。要讓它變成可站立的面，
> 等於是**新增一套與地面平行的碰撞系統**，不是把背景改成實心那麼簡單。這一項的程式成本被低估的風險最高。

★ 以上每一項都要**同步補進關卡編輯器**（HANDOFF §6），否則只能手改 JSON。
編輯器目前已經落後於程式（`scene.sky` / `reward_core` / `final` 等欄位都還沒支援），
**這批做下去之前先把編輯器補齊，會比事後一次補划算。**

### 15.4 【新增】敵人

| 敵人 | 內容 | 落點 |
|---|---|---|
| **隱形敵人** | 平時隱形，只在**短暫現身攻擊**時可被打到；裝偵測器則隨時可見可打 | 程式輕。**與 15.2 偵測器同一組** |
| **爬牆敵人** | 貼在側面牆上，所以玩家看到的是它的**頂面** | 依賴 15.3 側面牆面。★**要另一套朝向的圖** |
| **自爆敵人** | 衝過來自爆 | 最便宜:MINE 的爆炸邏輯 + 既有移動行為，程式近乎免費 |
| **衝擊敵人** | 撞擊會**把玩家推走**，可能推下懸崖 | 程式輕（機體加一個被推的位移）。★與 `pit` 的組合會很致命，難度要試 |

> 對照 §6：正式版原本規劃「**+3 種新行為型別**」。這裡一口氣列了 4 種，
> 且都不在 §6 原先的候選清單（射擊後撤退／空中投彈／召喚／護盾展開／地形穿越）內。
> **§6 的候選清單需要用這份重寫或合併**，不要兩份並存。

### 15.5 【新增】BOSS

| BOSS | 內容 | 落點 |
|---|---|---|
| **巨大 BOSS** | 在**側面露出上半身**的巨型機 | 設計已定案（2026-08-19），見 §15.5a。⚠️ 美術估算已推翻，見 §15.5a-7 |
| **高速飛行 BOSS** | 飛行、移動快 | 程式中等（新的移動狀態機）+ 圖 |

對照 §7：roster 理想值 5 隻，切片做 1 隻（OVERSEER 已完成）。這兩隻是 roster 的候選，**不是額外追加**。

---

#### ★ 15.5a 巨大 BOSS — 設計草案（2026-08-19 討論定案，未實作）

> ✅ **2026-08-19 骨架已實作完成**（`Source/entity_boss_parallel.lua`、`BOSS2`、測試關 `M011`）。
> 標【待試玩】者是只能靠實機手感決定的數值 —— **數值一律尚未調過**。
> ⚠️ **美術尚未製作，現在全部是程式繪製佔位**（白底黑框本體＋雙臂、黑色頭＋白眼）。
> 放圖即自動生效：`sprite` 給本體與頭、`arms.sprite` 給手臂（右臂自動鏡射）。
> ⚠️ 本節只寫**這一隻**。「每隻 BOSS 的攻擊方式個別討論」是使用者拍板的原則，
> 但下面 1～4 的**骨架**要一次做對，之後第二隻 BOSS 才會只是填資料。

**外型**：人形，畫面上只露出**上半身與雙臂**（下半身在畫面外）。頭部是弱點。

##### 1. ★★ 骨架改動：BOSS 從「序列制」擴充出「平行制」

現有 `boss_data.lua` + `entity_enemy.lua` 是**序列制**：
同一時間只有 `boss_parts[boss_phase]` 這一個零件存在，只有它可被打、會瞄準、會攻擊，
打爆才進下一個；血條顯示「當前零件的 HP」（`label n/3`）。

巨大 BOSS 需要**頭／左臂／右臂同時活著**、各有 HP、可個別爆掉 →
新增 `part_mode = "PARALLEL"`：

- 所有零件同時 alive，各自有 hp、命中框（沿用現有 `dx/dy/w/h` 本體座標系慣例）、攻擊計時器
- ★ BOSS 層級只有**一個 `attack_slot`**：誰的 cooldown 先到誰出手，出手期間其他零件不動作
  → **「雙臂攻擊時頭不發射」不必寫成特例，是排程器的自然結果**
- `SEQUENTIAL` 維持現況，**OVERSEER 完全不受影響**

**血條 UI**：平行制不沿用「n/3」。主血條＝**頭部**；手臂不給血條，
改用**外觀受損階段**表現（順便省掉一組 UI）。

##### 2. 勝負條件與「手臂是選擇」

**只有打爆頭才算擊倒 BOSS。** 手臂爆掉只是少一種攻擊 → 拆手臂是**選擇，不是流程**。

⚠️ 要防的坑：如果拆手臂純粹是減壓，最佳解永遠變成「先拆兩隻再打頭」，選擇就消失了。
因此設計成三條路都活著：

| 打法 | 結果 |
|---|---|
| **不拆手臂** | 速攻頭部。壓力最大、最快 |
| **拆掉一隻** | 甜蜜點：攻擊密度明顯下降 |
| **兩隻都拆** | ⚠️ 頭部進入**狂暴**（連射、開火窗口變短）—— 全拆有代價 |

手臂 HP 要夠高，讓「拆」真的要付出彈藥與時間（零件有耐久、資源有成本，代價是實的）。【待試玩】

##### 3. ★ 頭部怎麼打得到 —— 「不限制零件」如何真的成立

**拍板：原則上不限制裝備零件。** 可攻擊到高處頭部的手段：

| 手段 | 現況 |
|---|---|
| **CANON1/2/3** | ✅ 現成。`angle_min=0 / angle_max=90`、crank 調仰角 |
| **追蹤飛彈** | ✅ 現成。先上升再轉向追敵 |
| **高位槍** | ✅ 現成。拋物線（`projectile_grav_mult=18`） |
| **CLAW 甩投石頭** | ✅ 現成。投擲速度由甩臂角速度決定 |
| **場景砲台** | ✅ 現成，**見下方** |

⚠️ **漏洞**：只帶 GUN／GUN2／BACK_GUN（全部水平直射）的玩家打不到高處的頭
→ 名義上不限制零件，實際仍會卡死。

★★ **補法：戰場裡放一座可接管的場景砲台**（`scene.weapons`，S3 系統，現成）
走近按 A 接管 → crank 調 0~80° 仰角 → A 發射（`damage 10`、`grav_mult 40` 明顯拋物線）→ B 解除。

- **任何配裝都有辦法打到頭** → 「不限制零件」真的成立，且**不必為此更動頭部行為**
- ★ 代價是**接管期間玩家不控制機體、站在原地不能閃避** →
  拳擊與投石的威脅直接轉換成使用砲台的成本。**這是本戰鬥的核心風險／報酬結構**
- 一行程式都不用寫

**頭部下探**仍然保留，但角色改成 **telegraph**（開火預告）而非唯一解法：
頭平時在高處，**發射子彈時前傾／下探**，發射完縮回。
順帶讓「它打得到我、我也打得到它」的對稱性成立。

##### 4. 固定戰場（拍板：巨大 BOSS 不移動）

關卡 JSON 新增 `scene.arena = { x1, x2 }`：玩家跨過 `x1` 後鎖定，
相機停止捲動、玩家不能退出邊界。BOSS `move_speed = 0`。
**不填這個欄位的關卡完全不受影響**（OVERSEER 沿用現有巡邏）。

★ 附帶好處：**BOSS 不動 ＋ 戰場固定 ＝ 距離固定**
→ CANON 與砲台的 crank 仰角變成「學得會」的東西（玩家會記住某個角度打得到頭），
crank 瞄準從手忙腳亂升級成技術。

##### 5. 攻擊方式

| 部位 | 攻擊 | 備註 |
|---|---|---|
| **雙臂** | 握拳**往地面打擊** | ⛔ **不改變地形**（拍板）。只做範圍震波傷害＋畫面震動。<br>理由：`crumble` 塌陷不可逆，打久了戰場全是坑，後期反而拖死玩家。<br>★ **2026-08-19 修正 —— 三段式**：舉起後**左右追著玩家移動**（`track_range` 約一個本體寬）→ 追蹤停止、地面落點閃爍的**發招預告**（`warn`）→ 砸下。<br>★ 追蹤範圍刻意有上限：跑出去就打不到，**走位是有效解法**；無限追蹤＝必中＝沒得玩。<br>★ 預告的用途是**反應窗口**：追蹤停下來的那一刻才是「可以閃了」。 |
| **雙臂** | **投擲石頭** | 沿用現有 `Stone`，見第 6 項。<br>★ **2026-08-19 修正**：舊版是「固定速度往玩家方向丟」，實測**一律落在玩家前方地面、打不到人**。改成**解彈道**：算出「T 幀後落在玩家身上」的初速。<br>⚠️ Stone 是**逐幀**物理（`vy += g` 再 `y += vy`），要用離散解 `y_T = y0 + T·vy0 + g·T·(T+1)/2`；用連續版的 ½gT² 會系統性丟短。<br>★ 保留一點 `spread`：完全精確＝發發必中，玩家只能硬吃。 |
| **頭部** | 發射子彈 | ★ 雙臂攻擊期間**不發射**（由第 1 項的 `attack_slot` 保證） |

##### 6. 石頭：加 `owner` 欄位

現況 `Stone` **只對敵人有傷害**，對玩家完全無害。改為：

| owner | 行為 |
|---|---|
| `PLAYER` | 只傷敵人（**現況不變**） |
| `BOSS` | **飛行中只傷玩家** |
| （落地後） | ★ **歸零成中性** —— 否則落地的石頭會一直掛著 BOSS 的傷害判定 |

★ 落地即成為玩家的彈藥 → **BOSS 自己供應消耗品**，不必另外做補給機制。

##### ★ 6b. 臨時石頭（2026-08-19 拍板）

BOSS 丟出來的石頭是**有時限的消耗品**（`Stone.despawn_time`，一般關卡的石頭不設＝行為不變）：

| 情境 | 行為 |
|---|---|
| 落地 | **3 秒後消失**（最後 1 秒閃爍預告 —— 不預告就消失，玩家會以為是 bug） |
| 被爪子抓著 | **不倒數、不會在爪子上消失**。★ 計時只在「站在地上」時走，所以這條是自動成立的，不必特判 |
| 擊中 BOSS／敵人 | 立刻消失（永久石頭仍是「減速後繼續掉落」，既有關卡不受影響） |
| 丟出去沒打中 | 落地後**重新計時** |

★ 設計意圖：撿了要**馬上用**，不能囤一地。BOSS 供應的彈藥有時限，
才不會演變成「先站遠處囤十顆石頭再一次丟」——那會把戰鬥變成搬運作業。

##### 7. ⚠️ 美術估算：§15.5 原本的假設要推翻

§15.5 原本寫「畫面只放得下上半身，反而可能比全身 BOSS 省圖 —— 這點值得先確認」。
**2026-08-19 確認的結果：恐怕相反。** 格數只會更多，且每格尺寸大很多。

粗估：本體 1–2 格 ／ 頭 **3 格**（平常・開火下探・受損）／
手臂**一套** 3–4 格（待機・舉起・揮下／投擲）＋ 斷臂殘骸 1 格。

★★ **最大的省圖點：左右臂共用同一張圖、水平鏡射**（`mirror_when_right` 已有先例）
→ 手臂只需畫**一套**。

★ **手臂與頭各自獨立的小 imagetable，不要跟本體同一張表。**
現有慣例是「同一格尺寸、原位對齊」，本體若是 160×200，
手臂的每一格都會被撐成 160×200 —— 浪費且難畫。用 `dx/dy` 掛回本體座標系即可。

##### ★ 7b. 實作落點（2026-08-19）

| 檔案 | 內容 |
|---|---|
| `Source/entity_boss_parallel.lua` | **新檔**。平行制的全部邏輯：手臂代理實體、攻擊排程器、下探、狂暴、繪製、血條 |
| `Source/entity_enemy.lua` | `initBoss` 分流；`updateBoss` 整段分流；`drawBoss` 分流；抽出共用的 `drawPartExplosion` |
| `Source/entity_controller.lua` | 生成 BOSS 後把 `arm_proxies` 一起放進 `enemies` |
| `Source/entity_stone.lua` ／ `entity_mech.lua` | `Stone.owner`（見第 6 項） |
| `Source/state_mission.lua` | `scene.arena` 鎖定 + 相機夾制 + 玩家邊界 |
| `Source/boss_data.lua` | `BOSS2 = COLOSSUS` |
| `Source/levels/M011.json` | **測試關**「11 COLOSSUS TEST」。⚠️ 正式版要刪 |

⚠️ **編輯器尚未支援 `scene.arena` 與 `BOSS2`** —— 目前只能手編 JSON。
（編輯器已會保留未知欄位，所以重存不會把 `arena` 吃掉，但**建不出來**。）

##### 8. 其他已拍板的細節

- **斷臂殘骸整個消失**，不留在身上擋子彈 —— 否則拆手臂會有「自己製造掩體」的隱藏壞處
- **難度調整的觀測值是「玩家平均掉血比例」，不是命中次數。**
  耐久是**關卡結束後一次結算**的（`durability.lua`：`耗損 = 40 × 掉血比例²`，
  套用在所有裝備零件上，不分打到哪裡）。**平方**代表：
  打得漂亮幾乎免費，打得狼狽一場吃掉四成耐久。
  → 調頭部子彈的射速／數量，實際上調的是**維修帳單**。【待試玩】

### 15.6 【改制】CLAW 攻擊力

**現況**：CLAW 揮動就有攻擊力（`parts_data` 的 `attack = 5`）。

**構想**：**揮動時沒有攻擊力，用爪子「抓住」時才有攻擊力。**

**目的（設計意圖）**：製造取捨 —— 玩家得決定
「**先把手上的箱子放下來打，還是先移過去把箱子放好**」。
這與 §8.07 零件耐久的思路是同一類:**用取捨產生決策，而不是用數值產生變化。**

> ⚠️ 這會改變 DELIVER 類關卡（M008 那種運送任務）的節奏，屬於**已驗證手感的改動**。
> 需要實際玩過才知道「抓著箱子等於沒有攻擊手段」是有趣的壓力、還是單純的挫折。
> 退路:抓住時攻擊力**不為零但很低**，而不是完全沒有。

### 15.7 建議的先後順序（未定案，供討論）

1. **15.1 上層多零件** —— 幾乎所有東西的前提，且它的難點（焦點切換）要最早確認
2. **最便宜的驗證組**：高位槍 + 反向槍（零件）、自爆敵人（敵人）、鬆動地板（場景）
   —— 都是沿用既有行為換數值/位置，可以用來**量測「一個變體到底多便宜」**
3. **成對的功能**：隱形敵人 ＋ 偵測器（互相依賴，單獨做沒意義）
4. **兩個改制項各自單獨試玩**：GUN 改手動、CLAW 攻擊力 —— 不要混在其他批次裡，
   否則試玩回饋分不出是哪一項造成的
5. **最貴、最後再評估**：側面牆面 ＋ 爬牆敵人（新碰撞系統 + 另一套朝向的圖）、吊索 ＋ 吊索鉤（新移動模式）

### 15.8 需要你拍板的

- ✅ **輔助型零件是否一律不進焦點循環？** → **2026-08-12 拍板：混合制，每個零件用既有的 `operable` 欄位自己決定**
  （現在就在用：GUN=false 全自動、GUN2=true 手動）。
  ⚠️ 已告知的代價：**沒有一條可對玩家說明的規則**，玩家得逐個記住哪個要切換、哪個不用。
- ⬜ §6 原本的「+3 新行為型別」候選清單，要**用 15.4 取代**還是合併？
- ⬜ 15.5 的兩隻 BOSS 是**佔用 5 隻 roster 的名額**，還是把 roster 往上加？
- ✅ **這一整批相對於「量測 → 拍板」的位置** → **2026-08-12 拍板：本階段先不量產，先把功能與介面流程定下來**（見 §15.9）

### ★ 15.9 本階段執行批次（2026-08-12 拍板）

> **本階段不量產關卡。** 先把功能與介面的流程定下來，再進 Phase C。
> 理由見下面第 3 項——那正是「先做功能才會浮出來」的問題。

> ★ 時程面的對應章節是 [Schedule — Phase B.5](Schedule_正式版_組裝玩具任務.md)（兩份要一起維護）。

| # | 項目 | 狀態 |
|---|---|---|
| **0** | **HQ 版面重排 ＋ 流程改版**（§8.05b / §8.05c） | ✅ **完成**（2026-08-13） |
| **1** | **零件耐久：功能 + UI**（§8.07） | ✅ **完成** —— K=40 / R=0.3，實作在 `Source/durability.lua` |
| **2** | **新零件第一批：反向槍 + 高位槍** | ✅ **完成**（`gun_high.png` 仍是佔位圖）。高位槍 2026-08-19 改為拋射，見 §15.2。 |
| **3** | **配合新零件的敵人／場景** | ✅ **完成**（2026-08-13）—— PHANTOM 隱形敵人、吊索 `scene.ropes`、可破壞石塊（obstacle `hp`）、鬆動地板 `crumble`。移動地板延後、單獨做（見 §15.3）。 |
| **4** | **資源經濟：首過關獎勵 + 敵人掉落**（§8.08） | ✅ 完成（缺 `drop-table-8-8.png`） |
| **5** | **巨大 BOSS**（§15.5） | ✅ **骨架完成**（2026-08-19，§15.5a）。**數值未調、美術未做**（程式繪製佔位） |

##### ✅ 已結案（2026-08-19 拍板）

**「高位槍需不需要新的高處敵人？」→ 不需要，此問題作廢。**
高位槍已改成**拋射**武器（弧線彈道），打擊面來自「越過前方目標打到後面」，
不再依賴「站得高的敵人」這個前提。原本登記的候選方向（高台敵人／飛更高的敵人／
§15.4 爬牆敵人）**不因高位槍而需要**；它們若要做，要用自己的理由重新評估。

##### ⏭ 移交 Phase C（2026-08-19 拍板）

**資源數值平衡**（M001–M003 的獎勵曲線、掉落量、修理成本）**不在本階段處理**。
理由：獎勵曲線要對著完整關卡序列整體設計，關卡還沒量產就調等於白調。

##### ⚠️ 待補美術（程式都已接好，**放圖即自動生效，不必回頭改程式**）

| 檔名 | 規格 | 現況 |
|---|---|---|
| `gun_high.png` | 24×16（**不需要加高**，已改拋射） | 目前是 `gun.png` 的複本，只差外觀辨識度 |
| `drop-table-8-8.png` | 3 格：鋼 / 銅 / 橡膠 | 程式繪製佔位（方形／圓形／X） |
| `core1-table-64-64.png` **第 4 格** | 選中狀態 | 只有 3 格 → 焦點目前無視覺提示 |
| `core2/3-table-64-64.png` | 各 3~4 格 | 自動退回 `core1` |
| 耐久標記 | 8×8 或 12×12，兩種狀態 | 程式繪製佔位 |

> `core-table-24-24.png` 已無任何讀取端（核心移出組裝格後），可刪。

> ⚠️ 第 4 項**不在原始規劃裡**，是做耐久時發現「關卡獎勵無條件重複給、可無限刷」
> 才補上的洞（§8.07 早就點名「資源永遠夠用的話修理就沒有決策」）。
> 補洞又牽出死鎖安全閥與過關寬限兩項配套 —— 見 §8.08。
> **這個階段的範圍會隨實作而長大，因為它的本質就是「做出來才會知道哪裡沒想清楚」。**

#### 2. 第一批零件的規格（2026-08-12 量測）

兩個都是 **1 格寬、`operable = false`**（被動自動開火、不進焦點循環）
→ **切換壓力零增加，既有 8 關的難度基準不變。**

| 零件 | 程式改動 | 說明 |
|---|---|---|
| **反向槍**（往左射） | **5 處** | ① `parts_data` 新資料 ② [entity_mech.lua:756-773](Source/entity_mech.lua:756) 發射方向**寫死向右**（`vx = base_speed * mult`、擋位檢查寫死 `"RIGHT"`）→ 需加 `fire_direction` 欄位 ③ [state_hq.lua:172-190](Source/state_hq.lua:172) 槍口淨空只有 `requires_clear_right`，需**鏡像的左側版**（雙向檢查兩段都要） ④ [entity_mech_render.lua:241](Source/entity_mech_render.lua:241) 圖是 `px = 格子左緣` **左對齊**畫的，`gun.png` 24×16 只佔 1 格（16px），多出的 8px 就是往右伸的槍口；反向槍槍口要往**左**伸，左對齊會讓槍身反而侵入隔壁格 → **需新增 `image_offset_x`**（目前只有 `image_offset_y`） ⑤ ★ `image_offset_x` 的**套用範圍待定**——`align_image_top` 共 **7 處**在讀（§5-3），只改一處會變成「機體上正確、商店裡偏 8px」 |
| **高位槍**（打高處） | **2 處** | ① `parts_data` 新資料，用**既有的** `image_offset_y`（負值往上） ② [entity_mech.lua:759-760](Source/entity_mech.lua:759) 發射點是 `gun_y = 格子中心`、與圖無關 → 圖畫高了子彈仍從原位出來，要跟著 offset 走。**無新欄位、無版面連動** |

**好消息**：`isFiringDirectionBlocked()` 本身是**方向泛型**的，傳 `"LEFT"` 即可用；
子彈碰撞與物理也是速度泛型，往左飛不需額外處理。

**美術：合計 2 張**（正好驗證 [Schedule §Phase B](Schedule_正式版_組裝玩具任務.md) 那句「新零件只要 1 張圖」）

| 檔名 | 尺寸 | 內容 |
|---|---|---|
| `gun_back.png` | **24×16** | `gun.png` 的**水平鏡像**，槍口朝左 |
| `gun_high.png` | **24×16** | 槍口朝右，造型較細／較小，暗示攻擊力弱 |

面板**兩者沿用 `gun_panel.png`**（GUN2 已經是這樣用的）。

#### 3. ★★ 配合的敵人／場景 —— 沒有這項，兩把槍裝上去沒有用

| 零件 | 需要什麼才有意義 | 現況 |
|---|---|---|
| **反向槍** | 敵人出現在**左側／背後** | ❌ 所有敵人都在右側、`flip_x` 一律面向左。**玩家永遠往右推進，背後是空的** |
| **高位槍** | **高處**的目標 | ⚠️ 只有 DRONE 浮空，且它會下降追人 |

- 反向槍要成立，需下列**至少一種**：會繞到背後的敵人／**從左側生成的重生點**／需要折返的關卡結構
- 高位槍要成立，需：**固定高度不下降**的飛行敵人，或**站在高台上**的敵人

> ⚠️ 這是**先做零件才會浮出來的問題**，也是本階段「先定功能、不急著量產」最直接的回報 ——
> 若先量產 21 關，這兩把槍會做出來卻無處可用，關卡還得回頭重排。

#### 4. 巨大 BOSS 開工前要先確認的事

§15.5 留了一句「畫面只放得下上半身，**反而可能比全身 BOSS 省圖**——這點值得先確認」。
OVERSEER 是 `boss1-table-72-72` 共 **6 格**；巨大 BOSS 只露上半身則**格子遠大於 72×72**，
**格數少但每格大，總像素未必較省**。要在動筆前先估過。

---

## 16. 跨平台移植評估（🔒 **上架 Playdate Catalog 之後才啟動**）

> **這一節是封存的評估結果，不是計畫。**
> **啟動條件：Playdate 版已完成並上架 Catalog。** 在那之前不做任何移植工作、也不為了移植
> 而改變現在的架構 —— §14 的「切片 → 量測 → 拍板 → 量產 → 上架」還沒走完。
>
> 評估日 **2026-08-10**，數字量自當時的 codebase（10,905 行 Lua / 32 檔 / 61 PNG / 5 WAV / 8 關 JSON）。
> 重啟評估時**先重跑一次盤點**，因為 §15 那批功能會改變下面的結論（尤其 16.3）。

### 16.0 結論

**技術可行度高，但手機不建議當第一個擴展平台。**
若要離開 Playdate，**PC（Steam / itch.io）的成本約為手機的三分之一**，理由見 §16.6。

### 16.1 為什麼這個 codebase 特別好移植

引擎依賴其實只有五件事：**畫圖、畫字、讀按鍵、播聲音、存檔**。

| 項目 | 實測 | 評估 |
|---|---|---|
| Lua 邏輯 | 10,905 行 / 32 檔 | 🟢 幾乎全可留。遊戲邏輯是資料驅動（關卡 JSON、`parts_data`／`enemy_data`／`boss_data`），與平台無關 |
| `playdate.*` API 用量 | **僅約 25 個不同呼叫** | 🟢 API 面極窄 —— **這是最關鍵的一點** |
| `gfx.*` 繪圖 | 28 個不同呼叫，全是 2D immediate mode | 🟢 沒用到 Playdate 的 sprite／collision／animator 子系統，也沒有 shader／3D |
| 資產格式 | PNG / WAV / JSON | 🟢 全通用（`.pdi`／`.pdt` 只是編譯產物） |

→ 寫一層**約 30 個函式的相容層（shim）**架在另一個 Lua 引擎上，多數 `.lua` 檔可以原封不動。

### 16.2 需要實際動手的部分

| 項目 | site 數 | 說明 |
|---|---|---|
| `setImageDrawMode`（1-bit 繪圖模式） | **42** | `kDrawModeFillWhite` 這類是 Playdate 特有概念，別的引擎要用 shader／tint 模擬。**相容層裡最麻煩的一塊** |
| 硬寫的 400 / 240 | **51 處 / 15 檔** | 整片 letterbox（最省事）或全部改成變數 |
| `getSystemMenu`（Retry / Mission Select） | 3 | ⚠️ 手機沒有系統選單 → **要自己做暫停畫面**，含新 UI 美術 |
| `file.listFiles("levels/")` | 1 | 手機（尤其 Android APK）不一定能列舉打包資產 → 改成編譯期產關卡索引 |
| `sound.synth`（方波／鋸齒／雜訊） | 5 | 換成預錄樣本即可，最便宜 |
| 點陣字 `Assemble.fnt` | — | 要自己寫 renderer，約一個檔 |
| `datastore` 存檔 | 8 | 換成 JSON 寫進 app 目錄，最簡單 |

### 16.3 ★ crank 是簡單的；**焦點切換模型才是真正的風險**

**crank 只有 8 個呼叫點、5 種用途，全部都能對應到觸控且不會變差：**

| 用途 | 觸控對應 |
|---|---|
| CANON 瞄準（絕對角度）／場景砲台瞄準 | 圓盤旋鈕，手指繞著轉 |
| CLAW 手臂旋轉（相對量、有夾角上下限） | 弧形滑桿（有範圍限制反而更好做） |
| SWORD 角度（0–180） | 拖曳 |
| 選關捲動 | 慣性滑動 —— **觸控比 crank 好用** |

**但真正的問題在這裡：**

> 本作的核心張力是**焦點切換**（跳躍綁在焦點上，焦點在 CANON 時 A 是發射不是跳，見 §8.06）。
> 搬到觸控後**最自然的做法是每個零件給一顆專屬按鈕**——而那會讓切換壓力直接歸零，
> 遊戲會變成另一款遊戲。要保住這個張力，等於得在觸控上**刻意重建一個不方便**。

附帶條件：`buttonJustPressed` 散在 **13 檔 / 73 個呼叫點、沒有輸入抽象層**；
400×240 的操作面板已經很滿，再疊虛擬按鍵會擋畫面、手指也會遮住。
這一塊是**完整一輪的重新設計 + 一批新 UI 美術**，不是接線工作。

⚠️ **§15.1「上層可裝多個零件」會讓這件事更難** —— 零件愈多，觸控版愈難不走向「一顆零件一顆按鈕」。
若 §15 先做了，重啟本評估時這一節要重寫。

### 16.4 引擎選項

| 選項 | 語言 | 手機輸出 | 對本專案 |
|---|---|---|---|
| **Defold** | Lua | 🟢 一等公民、免費 | **首選**。Lua 邏輯可留，商店流程成熟 |
| **Solar2D** | Lua | 🟢 手機優先、免費 | 次選 |
| **LÖVE** | Lua | 🟡 能做但 build 要自己搞 | 語言最接近，但 iOS 上架流程得自己扛 |
| **Godot / Unity** | GDScript / C# | 🟢 最成熟 | ❌ 整個重寫，10,905 行全丟 |

Playdate SDK 本身不能帶走，但自己寫的程式碼沒有授權問題。

### 16.5 不建議手機的理由（非技術）

1. **市場落差**：Catalog 是策展制、買斷正常、競爭者以百計；iOS/Android 以百萬計，**付費買斷的獨立遊戲在手機上幾乎賣不動**，發現度是最大殺手。
2. **1-bit 的語境會反轉**：在 Playdate 上黑白是平台身份與魅力；在手機上同一批圖容易被讀成「畫得陽春」而非風格選擇。**對本案（美術是強項）特別不划算。**
3. **★ 成本落在弱項**：移植約 95% 是程式工作（相容層／輸入重構／觸控 UI），美術只需補虛擬按鍵與暫停畫面 —— 與本案的能力分布（§12：美術是瓶頸、程式靠 AI）正好相反。
4. **行政成本**：Apple US$99/年、Google US$25 一次，加上隱私政策、分級、各裝置尺寸的商店截圖。這些目前**都不在 §12 的盤點內**（§12 只算了 Playdate）。

### 16.6 ★ 若要離開 Playdate，第一站應該是 PC

同一套相容層工作，PC 版**省掉「觸控重新設計 + 虛擬 UI 美術 + 兩個手機商店的行政」**：

- 鍵盤直接對應方向鍵 + A/B，**焦點切換模型完整保留**，不必重新設計 → **§16.3 那個最貴、最有風險的問題直接消失**
- crank 用滑鼠滾輪或拖曳，一樣自然
- 400×240 整數放大 + letterbox 即可，不必處理各種比例
- 1-bit 復古在 PC 獨立圈是加分而非扣分

**→ 重啟本評估時的第一個問題不該是「要不要上手機」，而是「PC 還是手機」。**

> 下一步（若需要）：產出**分階段時程**（人日制），需你補「每週可投入工作天 + 預計開始日」才換算日曆。