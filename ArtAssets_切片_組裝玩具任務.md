# 美術素材清單 — 垂直切片（組裝玩具任務）

> 對應 [WBS 正式版](WBS_正式版_組裝玩具任務.md) D 區。範圍＝**垂直切片**所需，非全案 25 關。
> 目前所有新系統（S1–S10）功能已完成並以**程式佔位圖形**運作，本清單就是把佔位換成正式素材。
> 決策基準日：2026-07-30。

## 0. 通用規格

| 項目 | 規格 |
|------|------|
| 格式 | **1-bit PNG**（黑/白，透明背景）。Playdate 只顯示 1-bit |
| 螢幕 | 400 × 240 |
| 機體組裝格 | **1 格 = 16 × 16 px**。零件圖寬 = 佔用格數 × 16 |
| 操作面板格 | **1 格 = 32 × 32 px**，面板區 = 3 × 2 格 = **96 × 64 px** |
| 動畫（imagetable） | 檔名 `<名稱>-table-<單格寬>-<單格高>.png`，橫向排列<br>例：`enemy_jump-table-32-32.png`（3 幀 → 96×32） |
| 命名 | 全小寫、底線分隔；程式引用**不含 .png**（如 `images/enemy1`） |
| 放置位置 | `Source/images/`，放好後**重新編譯**即生效 |

**「接法」欄說明**
- **直接生效**：程式已在等這個檔名／或路徑寫在資料檔，放進去(或改路徑)就會用
- **需接線**：目前是程式畫的圖形，我要改程式讀圖（做好圖跟我說，一起接）

---

## 1. BOSS（★最重、最優先）

BOSS1「ASSEMBLY CORE」＝**核心 + 三個依序破壞的武器零件**。尺寸取自 [boss_data.lua](Source/boss_data.lua)，可改。

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 備註 |
|---|------|----------|------|------|------|
| 1 | 核心本體 | `boss1_core.png` | **56 × 64** | 需接線 | 沒有武器的軀幹；三階段共用 |
| 2 | 零件① 手臂機槍（完好） | `boss1_arm.png` | **26 × 26** | 需接線 | 階段1 的弱點 |
| 3 | 零件② 散射砲（完好） | `boss1_cannon.png` | **30 × 22** | 需接線 | 階段2 的弱點；階段1 時以「待打」狀態掛著 |
| 4 | 零件③ 內部武器 | `boss1_core_weapon.png` | **28 × 28** | 需接線 | **前兩階段隱藏**，打掉②才顯現 |
| 5 | 零件破壞殘骸（可選） | `boss1_broken.png` | 26 × 26 | 需接線 | 打爆後留在原位的焦黑殘件；不做則直接消失 |
| 6 | 弱點提示框（可選） | 程式畫 | — | — | 目前程式畫外框，夠用 |
| 7 | 爆炸動畫 | 沿用 `mine_explode-table-50-50.png` | — | 已可用 | 不必新畫 |

**設計提醒**:BOSS 移動與子彈速度已調成 ≈ 一般敵人（公平感），所以體型不需誇張巨大；重點是**看得出「零件一塊塊被拆掉」**。

---

## 2. 場景物件（新系統的佔位替換）

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 目前佔位 |
|---|------|----------|------|------|----------|
| 8 | 場景砲台 底座 | `turret_base.png` | **24 × 20** | 需接線 | 白底黑框方塊 |
| 9 | 場景砲台 砲管 | `turret_barrel.png` | **24 × 8** | 需接線 | 黑線（會依 crank 仰角旋轉，軸心在左端） |
| 10 | **砲台操作面板** | `turret_panel.png` | **96 × 64** | ✅**直接生效** | 程式已在找這個檔；缺圖時顯示佔位框 |
| 11 | 護送 NPC | `npc.png` | **20 × 24** | 需接線 | 白底黑框方塊 + NPC 字 |
| 12 | NPC 行走動畫（可選） | `npc_walk-table-20-24.png` | 20×24 ×3 幀 | 需接線 | MOVE 模式會自走，有動畫更好 |
| 13 | REACH 目標旗 | `goal_flag.png` | **24 × 48** | 需接線 | 線畫旗桿 + 三角旗 |
| 14 | 傳送點（出口） | `teleport.png` | **28 × 56** | 需接線 | 門框 + 箭頭（今天剛補上佔位繪製） |
| 15 | 懸崖邊緣（可選） | `cliff_edge.png` | 16 × 32 | 需接線 | 目前 pit 就是地面缺口，加邊緣岩壁更好讀 |

---

## 3. 開場過場 + 教學

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 備註 |
|---|------|----------|------|------|------|
| 16 | 開場 第1頁 | `intro1.png` | **400 × 240** | ✅**直接生效** | 改 [intro_data.lua](Source/intro_data.lua) 的 `image` 路徑即可 |
| 17 | 開場 第2頁 | `intro2.png` | 400 × 240 | ✅ 同上 | 目前三頁都用 `dialog_bg` 暫代 |
| 18 | 開場 第3頁 | `intro3.png` | 400 × 240 | ✅ 同上 | |
| 19 | 教學提示圖（可選） | `tut_*.png` | 自訂 | 需接線 | 目前教學是純文字框，已可用；要圖示再加 |

**文案**:開場在 [intro_data.lua](Source/intro_data.lua)、教學在 [tutorial_data.lua](Source/tutorial_data.lua),都可直接編修文字。

---

## 4. 新零件（C1 — 1 個「新功能」零件）

一個零件的完整素材組（以佔 2 格寬、可 crank 操作為例）:

| # | 素材 | 檔名慣例 | 尺寸 | 接法 |
|---|------|----------|------|------|
| 20 | 零件本體 | `<part>.png` | 佔用格數 × 16（2格＝**32 × 16**） | ✅ 直接生效（寫進 `parts_data.lua` 的 `image`） |
| 21 | 子件（如砲管/臂，若有） | `<part>_barrel.png` 等 | 依設計（如砲管 64 × 8） | 需接線（每種子件都要程式配合） |
| 22 | 操作面板 | `<part>_panel.png` | 佔用格數 × 32（2格＝**64 × 32**） | ✅ 直接生效（`ui_panel`） |
| 23 | crank 旋鈕（若用 crank） | `<part>_control.png` | **32 × 32** | 需接線 |
| 24 | A 鍵圖（若用 A） | `<part>_button-table-32-32.png` | 32×32 ×2 幀（放/按） | 需接線 |

**參考現有**:`canon.png 64×8` + `canon_base.png 32×16` + `canon_panel.png 64×32` + `canon_control.png 32×32` + `canon_button-table-32-32.png`。

---

## 5. 新敵人（C3 — 1 種）

| # | 素材 | 檔名建議 | 尺寸 | 接法 |
|---|------|----------|------|------|
| 25 | 敵人本體 | `enemy3.png` | **32 × 32**（參考 enemy1） | ✅ 直接生效（寫進 `enemy_data.lua` 的 `image`） |
| 26 | 動作動畫（可選） | `enemy3-table-32-32.png` | 32×32 × N 幀 | ✅ 直接生效（同 `image` 欄位指向 table） |
| 27 | 武器/子件（若有） | `enemy3_weapon.png` | 依設計 | 需接線 |

---

## 6. 關卡場景（C5 — 切片 3~4 關）

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 備註 |
|---|------|----------|------|------|------|
| 28 | 背景建物 ×3~4 | `bg_<theme>1..4.png` | **100~150 寬 × 100~250 高** | ✅ 直接生效（關卡 JSON 的 `backgrounds.image`） |
| 29 | 障礙物 | `obstacle_<theme>.png` | **32 × 32** | ✅ 直接生效（`obstacles.image`） |
| 30 | 石頭 / 目標箱（如需新樣式） | `stone2.png` / `target_box2.png` | 20×20 / 32×32 | ✅ 直接生效 | 現有已可用 |
**地面（不需美術素材；程式繪製）**

- 地面**維持純黑填充**。曾試過 8×8 `setPattern` 貼圖（含多樣式隨機交錯），實測效果不佳已移除——主因是 Playdate 的填充樣式**對齊螢幕座標**，鏡頭捲動時花紋不會跟著地面走，看起來會「滑動」。
- 改為在地形線下方加一條**表面層**（白色帶狀 + 上下黑線，預設厚度 **6px**），斜坡自動貼合。厚度可調:[entity_controller.lua](Source/entity_controller.lua) 的 `SURFACE_T`（或設 `controller.surface_thickness`）。
- 若之後想把表面層換成**圖片**（草皮/金屬紋）:需要 **64 × 6** 的橫向貼圖，斜坡處以旋轉貼合——要做再跟我說。

★**控量關鍵**:切片只做**1 個世界觀主題**的背景組（3~4 張建物），量產階段再依主題分組共用——這是把 25 關場景美術壓下來的主要手段。

---

## 6.5 現有素材與排版修正（2026-07-30 追加）

### A. 四個 UI 畫面

前三個畫面目前**完全是程式繪製、沒有底圖**。建議沿用 `hq_bg.png` / `shop_bg.png` 的成功模式:**你畫 400×240 底圖(含框線與標題字樣),我把內容對齊你的框** —— 這個分工在組裝介面與商店都很順。

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 現況 |
|---|------|----------|------|------|------|
| 31 | 遊戲標題畫面 | `cover.png`（覆蓋現有） | **400 × 240** | ✅**直接生效** | 已有圖,重繪即可;選項文字為程式繪製 |
| 32 | 選擇存檔畫面 底圖 | `save_bg.png` | **400 × 240** | 需接線 | 純程式畫。需 3 個存檔槽列 + BACK 的框位 |
| 33 | 選擇任務畫面 底圖 | `mission_select_bg.png` | **400 × 240** | 需接線 | 純程式畫。需任務列(4 列可視)+ 標題 + 進度/CLEAR 位 |
| 34 | 結算畫面 底圖 | `result_bg.png`（或勝/敗各一張 `result_win.png` / `result_lose.png`） | **400 × 240** | 需接線 | 純程式畫。需結果字樣、獎勵 3 行、下方選項列(NEXT/RETRY/SELECT) |

> 畫底圖時**框內留白**即可,文字與數值一律由程式填(才能顯示動態內容)。畫好後跟我說,我用像素量測對齊(同 hq_bg 的流程)。

### B. 現有素材尺寸/造型修正

| # | 素材 | 現況尺寸 | 修正方向 | 連動注意 |
|---|------|----------|----------|----------|
| 35 | CANON1 砲管 | `canon.png` **64 × 8** | **改短**（建議 40~48 × 8） | ✅ **程式自動跟上**:砲口發射點與 HQ 預覽都是讀圖寬算出來的,改短即自動對位 |
| 36 | CANON2 砲管 | `canon2.png` **64 × 12** | **改短**（建議 40~48 × 12） | ✅ 同上 |
| 37 | FEET | `feet.png` **48 × 32** | **改矮**（建議 48 × 24） | ✅ **踩地高度自動調整**（程式用 `圖高 − 格高` 算額外高度）<br>⚠️ **但走路動畫檔名含尺寸**:`feet_walk-table-48-32.png` → 改矮後要重畫並改名為 `feet_walk-table-48-24.png` |
| 38 | 爆炸特效 | `mine_explode-table-50-50.png`（3 幀） | 重繪/加幀 | ✅ 幀數不限（程式讀 `getLength()`）,改名的數字要對應單格尺寸<br>此表**地雷/敵人死亡/BOSS 死亡共用** |
| 39 | 飛行敵人 DRONE | ❌ **目前借用 `enemy1.png`**（與地面敵人同圖） | **新繪專屬圖** `drone.png` **32 × 32**（含旋翼/推進器辨識度） | ✅ 直接生效（改 `enemy_data.lua` 的 `image`）<br>建議做 `drone-table-32-32.png` 讓旋翼會動 |

**額外建議**:CANON 砲管改短後,`canon_base.png`(32×16)與砲管的比例會變,順便一起看是否要調底座。

---

## 7. 建議製作順序（先解鎖驗證、再美化）

1. **快速見效組（放檔即生效）**:`turret_panel.png`（#10）、開場三頁（#16–18）、`cover.png` 重繪（#31）
2. **小修正組（便宜、改善手感辨識）**:CANON 砲管改短（#35–36）、FEET 改矮（#37）、DRONE 專屬圖（#39）
3. **BOSS 五件（#1–5）** — 全案最重，越早開工越好；做完我一次接線
4. **場景物件（#8–9, 11, 13–14）** — 砲台/NPC/旗/傳送門，讓新機制不再是方塊
5. **UI 底圖三張（#32–34）** — 存檔/選任務/結算；每張畫完我就對齊一張
6. **新零件（#20–24）+ 新敵人（#25–26）+ 爆炸特效（#38）**
7. **場景背景組（#28–29）**

## 8. 數量小結（切片）

| 分區 | 必要 | 含可選 |
|------|------|--------|
| BOSS | 4 | 5 |
| 場景物件 | 6 | 8 |
| 開場/教學 | 3 | 4+ |
| 新零件 | 2~5 | 5 |
| 新敵人 | 1 | 3 |
| 場景背景 | 4~5 | 5 |
| **UI 畫面（#31–34）** | **4** | 5（結算勝/敗分開） |
| **現有素材修正（#35–39）** | **5** | 7（含動畫表） |
| **合計** | **約 29~34 張** | **約 40 張** |

> 對照 [Schedule](Schedule_正式版_組裝玩具任務.md):切片美術估 15~24 人日,與此數量相符。實際畫完的張數與時間請記錄下來——那就是 **QA2 單位成本量測**的原始資料,決定 25 關/5 王是否下修。
