# 美術素材清單 — 垂直切片（組裝玩具任務）

> 對應 [WBS 正式版](WBS_正式版_組裝玩具任務.md) D 區。範圍＝**垂直切片**所需，非全案 25 關。
> 目前所有新系統（S1–S10）功能已完成並以**程式佔位圖形**運作，本清單就是把佔位換成正式素材。
> 決策基準日：2026-07-30。

## 0. 通用規格

| 項目 | 規格 |
|------|------|
| 格式 | **1-bit PNG**（黑/白，透明背景）。Playdate 只顯示 1-bit |
| 螢幕 | 400 × 240 |
| 機體組裝格 | **1 格 = 16 × 16 px**。零件圖寬 = 佔用格數 × 16 |
| 操作面板格 | **1 格 = 32 × 32 px**，面板區 = 3 × 2 格 = **96 × 64 px** |
| 動畫（imagetable） | 檔名 `<名稱>-table-<單格寬>-<單格高>.png`，橫向排列<br>例：`enemy_jump-table-32-32.png`（3 幀 → 96×32） |
| 命名 | 全小寫、底線分隔；程式引用**不含 .png**（如 `images/enemy1`） |
| 放置位置 | `Source/images/`，放好後**重新編譯**即生效 |

### ★ 過場圖(開場/結局/關卡 dialog)的規格 = 400 × 163

**出圖尺寸就是 400 × 163**(2026-08-05 改;原為出 400×240 但下半看不到,白畫)。程式從 (0,0) 貼,
y=163 以下是滿版白底文字框,**圖不必畫到那裡**。

```
y=0   ┌─────────────────────────┐
      │                         │
      │   ★ 圖 = 400 × 163       │
      │      (整張都看得到)      │
y=163 ├─────────────────────────┤ ← 滿版白框(x=0~400)
      │  打字機文字              │
      │  A: next   B: skip  1/4 │
y=240 └─────────────────────────┘
```

出處(三處必須一致,各檔的 `DIALOG_Y = 163`):
[state_intro.lua](Source/state_intro.lua)、[state_outro.lua](Source/state_outro.lua)、[state_mission.lua](Source/state_mission.lua)。

**★ 文字可用寬度同步變成 384px**(原 364px)——文字框從 380 寬擴成滿版 400，扣左右 padding 16。
既有九句最寬 328px,不受影響。

> **⚠️ 2026-08-04~05 的兩次修正紀錄**
> 1. 最早寫「y=136 以下畫什麼都看不到」是**錯的**——文字框只蓋到 216,下方提示列會露出圖。
>    使用者把 intro_02/03 下半畫成純黑,黑色提示字就消失了。已改程式在框以下整塊鋪白。
> 2. 既然下半永遠看不到,出圖尺寸就從 400×240 縮成 **400×163**,同時把文字框擴成滿版寬度,
>    插圖可視高度反而從 136 增加到 **163**。

### ★★ 造型語彙:軍規 vs 玩具規格(2026-08-03 定案)

依 [GDD §1.5](GDD_正式版_組裝玩具任務.md)。**本清單所有素材都適用**,畫任何一張圖前先確認它屬於哪一邊。

| | **軍規**(敵人/BOSS/場景砲台) | **玩具規格**(主角/零件/工作機器) |
|---|---|---|
| 填色 | **黑色實心**為主 | **白底黑線稿**,內部留白 |
| 接合處 | **焊道**(不規則接縫) | ★ **可見的圓形卡榫/球窩關節** |
| 螺絲 | 無 | **可見螺絲點** |
| 旋鈕 | 無 | ★ **發條孔/旋鈕**(crank 的實體對應) |
| 輪廓 | 銳角、瘦長、傾斜 | 圓角、粗胖、分件明顯 |
| 一句話 | 看不出怎麼拆開 | 看得出是拼起來的 |

**為什麼這樣分**:1-bit + 400×240 上,細節會消失,但「填色/輪廓/接點」層級的差異不會。
**副產品**:玩家得到瞬間辨識規則 —— **看得到接點的是同類,看不到接點的要打**。

**★ 現況:已經做到一半**
`enemy1` / `enemy2` 已是黑色實心剪影(=軍規),`wheel` / `feet` 已是白底線稿含細節點(=玩具規格)。
所以這不是重畫,是**把既有差異推到自覺、推到極端**。

**★ 造型改動不改尺寸 → 程式零成本**
`canon.png` 仍是 64×8,砲口位置照樣自動算。唯一例外是檔名含尺寸的動畫(如 `feet_walk-table-48-32`),那本來就在 §6.5 B 待修清單裡。

**三個坑**
1. 粗胖 ≠ Q 版 —— 玩具規格畫太可愛會跟目標感受「緊張爽快」打架。保持機械感,只是**可拆解的機械**
2. 敵人別畫成純黑塊 —— 銳角化時要留輪廓辨識度,否則遠看分不出敵人種類
3. 別靠文字解釋 —— 規格分類全靠造型傳達,文案只在 GDD 指定的四個位置各點一次

**「接法」欄說明**
- **直接生效**：程式已在等這個檔名／或路徑寫在資料檔，放進去(或改路徑)就會用
- **需接線**：目前是程式畫的圖形，我要改程式讀圖（做好圖跟我說，一起接）

---

## 0.5 ★ 完成進度總覽（2026-08-05 盤點）

以 `Source/images/` 實際檔案為準（共 57 張 PNG）。

### ✅ 已完成並接線

| 項目 | 檔案 | 備註 |
|---|---|---|
| BOSS1 全套 | `boss1-table-72-72.png`（**6 格**） | **2026-08-11 改版**:本體/管子/履帶/槍/canon/雷射槍。**實作成單一 imagetable**,非本清單原訂的 #1–5 分張<br>★ 原第 4 格「前輪」取消,後輪+前輪合併成**一整條履帶**(不旋轉)<br>★ **顯示雷射槍時本體隱藏**,畫面只剩履帶＋雷射槍 |
| 場景砲台 | `turret-table-32-32.png`（2 格）+ `turret_control.png` | 底座+砲管一表;面板實際檔名是 `turret_control`(32×32),非原訂的 `turret_panel`(96×64) |
| 護送 NPC | `npc_walk-table-36-36.png`（2 格） | 2026-08-05 由 24×28 換新 |
| 傳送點 | `arrow-table-32-32.png`（3 格） | |
| Bunker | `bunker.png`（32×32） | 原清單漏列 |
| 前景裝飾 | `forground-table-32-32.png`（4 格） | 注意拼法是 `forground` |
| **開場四頁** | `intro_01`~`intro_04.png`（**400×163**） | 核爆 / 廢墟 / 核心特寫 / 規格對峙。文案已定稿 |
| **天空層** | `bg_sky.png`（400×160） | 新系統 `scene.sky`,12 個場景全部接上 |
| **背景建物 ×7** | `bg_building1~7.png` | 2026-08-11 重繪並由 4 張擴充到 **7 張**（1~3 為 128×128、4~6 為 64×64、7 為 128×32）。已不再是暫用品,#28 結案 |
| **核心 CORE 三階** | `core-table-32-16.png`（3 格 32×16） | 2026-08-06 放檔、08-07 加大並**系統全部接線完成**（HP／負重上限／跳躍加成／升級畫面） |
| **CANON 砲管 ×2** | `canon.png` / `canon2.png`（皆 40×16） | 2026-08-08 改短。`canon3.png`（迫擊砲）同尺寸 |
| **FEET** | `feet.png`（48×20）+ `feet_walk-table-48-20.png`（4 格） | 2026-08-07 改矮 |
| **爆炸特效** | `mine_explode-table-50-50.png`（3 格） | 2026-08-07 重繪。地雷／敵人死亡／BOSS 死亡共用 |
| **飛行敵人 DRONE** | `enemy_drone-table-32-32.png`（6 格） | 2026-08-09 專屬圖，`anim_fps = 12` |
| **揮劍敵人 SWORD** | `enemy2.png`（32×32）+ `enemy2_sword.png`（48×16） | 2026-08-10 重繪。身體 40→32 高;劍軸心重量測（見 §6.5 #40） |
| **地雷 MINE** | `mine-table-32-16.png`（3 格） | 2026-08-10 由單張改 3 格:本體＋警示燈閃爍（見 §6.5 #41） |
| **CLAW 開合開關鈕** | `claw-button-table-32-32.png`（2 格） | 2026-08-10 取代佔位圖。**1=開 / 2=夾起**（見 §6.5 #42） |
| **盾牌（SHIELD_ROBOT）** | `shield.png`（16×32） | 2026-08-12 取代程式繪製的黑底白框。★ `shield_*` 同時是繪製位置與擋彈判定框 |
| **走路敵人 WALKER** | `enemy04-table-40-32.png`（3 格） | 2026-08-12。行為＝移動較快、**停下才開火**。**本切片最後一種新敵人** |
| **WHEEL2** | `wheel2.png`（48×20） | 2026-08-10 重繪加高，改用 FEET 的上緣對齊（見 §6.5 #43） |
| **標題畫面** | `cover.png`（400×240，**透明底**） | 2026-08-11 只留標題字;下層是程式生成的**零件 4 排橫向捲動**（見 §6.5 #31） |
| **存檔畫面底圖** | `save_bg.png`（400×240） | 2026-08-11 接線。**選關與結算畫面也暫用這張**（見 §6.5 #32／#33／#34） |
| **雷射槍 GUN2** | `gun02.png`（32×16） | 2026-08-11 完成並接線。**本切片唯一的新零件，C1 結案**（見 §4）<br>面板／A 鈕沿用既有的 `gun_panel` + `canon_button`，不需新畫 |

### ⬜ 待製作（依建議順序）

| 優先 | 項目 | 張數 | 編號 |
|---|---|---|---|
| ~~1~~ | ~~核心 CORE 三階~~ → ✅ **圖已完成**（`core-table-16-16.png`），但**系統待實作** | ~~3~~ | #20a–c |
| ~~1~~ | ~~結局過場三張~~ → ❌ **2026-08-07 取消**，改為全部共用既有的 `dialog_bg` | ~~3~~ → 0 | #16b–d |
| ~~3~~ | ~~現有素材修正:CANON×2 改短、FEET 改矮、爆炸特效、DRONE 專屬圖~~ → ✅ **2026-08-10 全數完成並接線** | ~~5~~ → 0 | #35–39 |
| ~~—~~ | ~~揮劍敵人 SWORD_ENEMY 重繪~~ → ✅ **2026-08-10 完成並接線**（劍軸心已重新量測） | ~~1~~ → 0 | #40 |
| ~~—~~ | ~~地雷 MINE 改 3 格（本體＋警示燈閃爍）~~ → ✅ **2026-08-10 完成並接線** | ~~1~~ → 0 | #41 |
| ~~—~~ | ~~CLAW 開合開關鈕專屬圖（取代佔位）~~ → ✅ **2026-08-10 完成並接線** | ~~1~~ → 0 | #42 |
| ~~—~~ | ~~WHEEL2 重繪（48×16 → 48×20，改上緣對齊）~~ → ✅ **2026-08-10 完成並接線** | ~~1~~ → 0 | #43 |
| ~~4~~ | ~~UI 底圖:`cover` 重繪 + 存檔~~ → ✅ **2026-08-11 完成並接線** | ~~2~~ → 0 | #31 / #32 |
| ~~4~~ | ~~UI 底圖：結算畫面~~ → 🟡 **沿用 `save_bg` 暫代，不另畫** | ~~1–2~~ → 0 | #34（#33 同） |
| **5** | 場景物件：懸崖邊緣（可選，非必要） | 0–1 | #15 |
| ~~6~~ | ~~新零件一組~~ → ✅ **2026-08-11 完成並結案**（雷射槍 `GUN2`） | ~~2–5~~ → 0 | #20–24 |
| ~~7~~ | ~~新敵人~~ → ✅ **2026-08-12 完成並結案**（WALKER） | ~~1–3~~ → 0 | #25–27 |
| ~~8~~ | ~~障礙物~~ → ⏸ **2026-08-12 延後至下階段** | ~~1~~ → 0 | #29 |
| ~~—~~ | ~~結局專屬圖 `cut_ending`~~ → ⏸ **延後至下階段** | ~~1~~ → 0 | #16 |
| 可選 | 教學提示圖 | — | #19 |

### ✅ 切片美術已結案（2026-08-12）

**必做項目剩餘 0 張。** 最後一張是走路敵人 `enemy04-table-40-32.png`。

| 收尾決定（2026-08-12 使用者拍板） | |
|---|---|
| 新零件 C1 | ✅ 結案——就雷射槍 `GUN2` 一個 |
| 新敵人 C3 | ✅ 結案——就 WALKER 一種 |
| 選關／結算底圖 | 🟡 沿用 `save_bg` 暫代，不另畫 |
| 障礙物、結局專屬圖 | ⏸ 延後至下階段 |

**關卡 M001–M003 已完成**（資源數值待調）。
→ 下一步：**實機測試 → 檢討 → QA2 工時評估**。

> ★ 這些**實際花費的時間請記錄**——那就是 **QA2 單位成本量測**的原始資料，決定 25 關/5 王要不要下修。

---

## 0.6 ★★ M2.5 待製作清單（2026-08-19 盤點，**現在要做的就是這一份**）

> 切片的素材已於 2026-08-12 結案（§0.5）。這一份是 **M2.5 期間新做的功能**留下的佔位。
> **除了最後一項「耐久標記」以外，全部都是「放圖進 `Source/images/` → 重新編譯 → 立刻生效」**，
> 不必回頭改任何程式（BOSS 的 sprite 路徑已於 2026-08-19 預先指好）。
> 通用規格見 §0（1-bit PNG、透明背景、imagetable 檔名 `<名稱>-table-<單格寬>-<單格高>.png`）。

### A. 零件（組裝格 1 格 = 16×16）

| 檔名 | 尺寸 | 現況 / 注意 |
|---|---|---|
| `auto_loader.png` | **16×16** | 新零件 AUTOLOADER（1 格、被動）。沒圖時走方塊佔位 |
| `gun_high.png` | **24×16** | 目前是 `gun.png` 的**複本**，只差辨識度。<br>★ **不需要加高** —— 高位槍已改成拋射武器，「高」不再靠圖的高度 |

### B. 掉落物

| 檔名 | 尺寸 | 內容 |
|---|---|---|
| `drop-table-8-8.png` | **8×8 × 3 格** | 1=鋼　2=銅　3=橡膠。現在是程式繪製佔位（方形／圓形／X） |

### C. 敵人

| 檔名 | 尺寸 | 現況 / 注意 |
|---|---|---|
| CRAWLER（爬牆） | 建議 **32×32**（命中框 26×26） | 現借用 `enemy_drone-table-32-32`。<br>★ **玩家看到的是它的頂面** —— 它貼在側面牆上，朝向與一般敵人不同 |
| BOMBER（自爆） | 現借用 `enemy1-table-38-32`（BASIC 的圖） | ★ 需要「快爆了」的辨識度。<br>做成 **3 格 imagetable**（1=本體／2、3=警示燈交替）就能沿用 MINE 的閃燈機制 —— 告訴我格號我補 `warn_frame_a/b` 兩行資料 |
| RAMMER（衝擊） | 現借用 `enemy02`（32×32） | 體型應比 BASIC 壯，讀得出「它會撞過來」 |

### ✅ 已完成（2026-08-20）

| 檔名 | 規格 | 說明 |
|---|---|---|
| `enemy1-table-38-32.png` | 38×32 **× 2 格** | 取代原本的單張 `enemy01.png`。**1=待機／2=移動**。<br>使用者：BASIC_ENEMY／PHANTOM／BOMBER（後者仍是佔位）。<br>★ 換幀由 `anim_idle_move` 驅動（看「這一幀 x 有沒有變」），**不要設 `anim_fps`** —— 那是無條件循環，會讓它站著也在走路。 |

⚠️ **實測發現：BASIC 與 PHANTOM 幾乎不會出現待機格。**
它們的 `MOVE FORWARD/BACK` 是**持續移動**、只是每 2 秒可能換方向，從來不會停下來。
待機格目前只有 **BOMBER 停下來倒數自爆**時會用到。
→ 要讓待機格真的看得到，得給它們**走走停停的節奏**（像 WALKER 那樣），
但那會改變**所有既有關卡**的敵人行為，屬於要先拍板的事，**尚未做**。

### D. BOSS

**COLOSSUS（巨大 BOSS，§15.5a）**

| 檔名 | 規格 | 格內容 |
|---|---|---|
| `boss2-table-130-150.png` | 130×150 **× 3 格** | 1 本體(上半身含肩，**不含**頭與手臂)／2 頭(平常)／3 頭(開火下探) |
| `boss2_arm-table-30-100.png` | 30×100 **× 2 格** | 1 待機／2 舉起・揮下 |

- ★★ `boss2` 的**每一格都是整張 130×150 的畫布，只畫該部位、其餘透明**（與 `boss1` 同慣例）。
  頭的下探是「整格往下移」，所以頭一定要畫在它平常的位置上。
- ★★ **手臂左右共用一套** —— 右臂由程式水平鏡射，**只要畫一套**。這是本批最大的省圖點。
- 手臂的格子是**手臂尺寸**（30×100），不是本體尺寸 —— 它是獨立的 imagetable。
- 版面：本體底邊停在**地面線**上（`ground_y − 64`，M011 是 156）→「只露上半身」不是靠裁切。

**COMET（高速飛行 BOSS，§15.5b）**

| 檔名 | 規格 | 格內容 |
|---|---|---|
| `boss3-table-64-40.png` | 64×40 **× 4 格** | 1 本體／2 ENGINE／3 PODS／4 CORE |

- ★ 同樣是「整張 64×40 的畫布、只畫該部位、其餘透明」。
- ⚠️ **弱點的命中框已經寫在 `boss_data.lua` 裡，畫的時候要對齊**（否則會「看起來打到卻沒扣血」）：
  `ENGINE (44,8) 20×18`／`PODS (8,22) 34×14`／`CORE (24,12) 22×20`。
  你想畫在別的位置也可以 —— **告訴我實際位置，我改資料就好**，不要遷就數字把圖畫醜。

### E. 場景

| 項目 | 規格 | 注意 |
|---|---|---|
| 牆壁背景（給 CRAWLER） | 任意尺寸 | 放進 `backgrounds`，★ **`parallax` 必須填 `1.0`** —— 不填的話牆會視差捲動，鏡頭一動就跟爬牆軌道分家 |

### F. ⚠️ 唯一「放圖不會自動生效」的一項

| 項目 | 規格 | 說明 |
|---|---|---|
| 耐久標記 | **8×8，兩種狀態**（實心＝已損壞／空心＝快壞了） | 目前是 [state_hq.lua](Source/state_hq.lua) 約 1487 行的**純程式繪製，沒有讀圖端**。<br>畫好之後跟我說，我加載入程式（幾行）。位置是零件佔用格的右上角 |

### ✅ 更正兩則過期資訊（2026-08-19 查證）

- **核心按鈕已經做完了**：`core1/2/3-table-80-80.png` 三張都存在且**各 5 格**。
  GDD 舊寫的「`core1-table-64-64` 第 4 格未做 / core2・core3 未做」**已過期**，不用畫。
- `core-table-24-24.png` 已無任何讀取端，可刪（這條仍然有效）。

---

## 1. BOSS（★最重、最優先）

BOSS1「ASSEMBLY CORE」＝**核心 + 三個依序破壞的武器零件**。尺寸取自 [boss_data.lua](Source/boss_data.lua)，可改。

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 備註 |
|---|------|----------|------|------|------|
> ★ **本節 #1–5 已由 `boss1-table-72-72.png` 一張 imagetable 取代並完成接線**
> （2026-08-11 改版為 **6 格**:1 本體 / 2 管子 / 3 履帶 / 4 槍 / 5 canon / 6 雷射槍）。
> 下表保留作**設計意圖說明**與未來 BOSS2–5 的規劃參考,不必再照它分張出圖。

| 1 | 核心本體 | ~~`boss1_core.png`~~ | **56 × 64** | ✅ 已完成 | 沒有武器的軀幹；三階段共用 |
| 2 | 零件① 手臂機槍（完好） | ~~`boss1_arm.png`~~ | **26 × 26** | ✅ 已完成 | 階段1 的弱點 |
| 3 | 零件② 散射砲（完好） | ~~`boss1_cannon.png`~~ | **30 × 22** | ✅ 已完成 | 階段2 的弱點；階段1 時以「待打」狀態掛著 |
| 4 | 零件③ 內部武器 | ~~`boss1_core_weapon.png`~~ | **28 × 28** | ✅ 已完成 | **前兩階段隱藏**，打掉②才顯現 |
| 5 | 零件破壞殘骸（可選） | `boss1_broken.png` | 26 × 26 | ⬜ 未做 | 打爆後留在原位的焦黑殘件；不做則直接消失（現行＝直接消失） |
| 6 | 弱點提示框（可選） | 程式畫 | — | — | 目前程式畫外框，夠用 |
| 7 | 爆炸動畫 | 沿用 `mine_explode-table-50-50.png` | — | 已可用 | 不必新畫 |

**設計提醒**:BOSS 移動與子彈速度已調成 ≈ 一般敵人（公平感），所以體型不需誇張巨大；重點是**看得出「零件一塊塊被拆掉」**。

---

## 2. 場景物件（新系統的佔位替換）

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 目前佔位 |
|---|------|----------|------|------|----------|
| 8–9 | 場景砲台（底座＋砲管） | `turret-table-32-32.png` | **32 × 32 ×2 幀** | ✅**已完成並接線** | 實作成一張 imagetable（原訂分成 `turret_base`/`turret_barrel` 兩張,未採用）。第 2 格砲管依 crank 仰角旋轉 |
| 10 | **砲台操作面板** | `turret_control.png` | **32 × 32** | ✅**已完成並接線** | ★實際檔名與尺寸與原訂的 `turret_panel`(96×64) **不同**——程式讀的是 `images/turret_control`（[state_mission.lua:1149](Source/state_mission.lua#L1149)），面板其餘格填 `empty` |
| 11–12 | 護送 NPC（走路動畫） | `npc_walk-table-36-36.png` | **36 × 36 ×2 幀** | ✅**已完成並接線** | 2026-08-05 由 24×28 換成 36×36。**靜圖 `npc.png` 不需要**，程式只讀這張 imagetable（載入失敗才退回白底黑框佔位） |
| 13 | REACH 目標旗 | ~~`goal_flag.png`~~ → 暫用 `bunker.png` | 32 × 32 | ✅**已接線（暫代）** | 2026-08-06:**REACH 與 bunker 功能相同**（走到定點就達標），決定共用同一張圖，不另外畫旗子。原本的程式繪製旗桿+三角旗已移除。要各自專屬圖時改 `EntityController:drawGoalMarker()` 的圖片路徑成參數即可 |
| 14 | 傳送點（出口） | `arrow-table-32-32.png` | **32 × 32 ×3 幀** | ✅**已完成並接線** | 以動畫箭頭實作（原訂 `teleport.png` 門框未採用） |
| 14b | Bunker（護送終點） | `bunker.png` | **32 × 32** | ✅**已完成並接線** | 原清單漏列；PROTECT-MOVE 的終點 |
| 15 | 懸崖邊緣（可選） | `cliff_edge.png` | 16 × 32 | ⬜ 需接線 | 目前 pit 就是地面缺口，加邊緣岩壁更好讀 |
| 15c | **CLAW 開合開關（面板左格）** | `claw_control_v-table-32-32.png` | 32 × 32 ×N 幀 | ✅**放檔即生效** | 2026-08-08 接線：**第 1 格＝關(閉合)、第 2 格＝開**，反映 `claw_is_closed`。目前沿用既有 3 格圖當佔位（第 3 格未使用）。重畫時**只要維持 1=關 / 2=開 的順序**，程式不用改 |
| 15b | **子彈命中火花** | `hit_spark-table-<寬>-<高>.png` | 建議 **16 × 16 ×4 幀** | ✅**放檔即生效** | 2026-08-08 先以**程式繪製**上線（擴散環＋四向放射線，黑線白描邊）。放圖進 `Source/images/` 就自動改用圖，**幀數由 imagetable 長度決定**，程式與呼叫端都不用改。每幀 0.04 秒 |

---

## 3. 過場圖 + 教學（2026-08-01 依定案劇情改寫）

> 劇情定案見 [GDD §1.5](GDD_正式版_組裝玩具任務.md)。**全機器人世界、無人類 → 不必畫人**。
> ★**控量策略(2026-08-07 定案):切片做 6 張 —— 開場 4 張 + 通用 1 張 + 結局專屬 1 張。**
> 一般關卡**全部共用那 1 張通用圖,只換文字、不換圖**;**結局是唯一的例外,有專屬圖**。
>
> | 版本 | 通用過場圖 | 說明 |
> |---|---|---|
> | **垂直切片(現在)** | **1 張通用 + 1 張結局** | 一般關卡共用既有的 `dialog_bg.png`;結局用專屬的 `cut_ending.png` |
> | 量產(25 關) | 每章節 1 張 + 結局 | 3~4 關一組共用;若時程吃緊，一般關卡可全案回退成 1 張,**但結局的專屬圖不要砍** |
>
> 沿革:6 張(08-01) → 7 張(08-03 加核爆頁) → 5 張(08-07 通用 3 張砍成 1 張) → **6 張(08-07 結局加回專屬圖)**。
> 淨結果:原訂 `cut_field` / `cut_survivor` 取消,`cut_core` 改名為 `cut_ending` 並限定給結局。

**全部規格**:**400 × 163 出圖**,整張都看得到(見 §0)。

| # | 素材 | 檔名 | 接法 | 構圖建議 |
|---|------|------|------|----------|
| 15 | 開場 第1頁 | `intro_01.png` | ✅**已完成並接線** | **核爆**:蘑菇雲 + 城市天際線 + 道路消失點。已於 2026-08-04 放檔接線 |
| 16 | 開場 第2頁 | `intro_02.png` | ✅**已完成並接線** | **廢墟街景**:倒塌大樓與瓦礫。同上 |
| 17 | 開場 第3頁 | `intro_03.png` | ✅**已完成並接線** | **主角核心特寫**,躺在零件堆裡。同上 |
| 18 | 開場 第4頁 | `intro_04.png` | ✅**已完成並接線** | **兩種規格對峙**:左＝軍規(黑實心/履帶/砲管)、右＝玩具規格(白線稿/可見關節)。★使用者改成對峙構圖,比原訂的「站起來剪影」更直接畫出 [GDD §1.5](GDD_正式版_組裝玩具任務.md) 的規格分類 |

| 16b | **通用過場圖（唯一一張）** | `dialog_bg.png` | ✅**已完成並接線** | ★**全部 12 處關卡對話 + 結局 3 頁都指向它**。目前是機器內部/電路板的抽象構圖——中性,任何關卡都不違和,這正是它能共用的原因<br>⚠️ 現況是 400×240（舊規格）,重繪時請出 **400×163** |
| ~~16c~~ | ~~通用:工作機器聚落~~ | ~~`cut_survivor.png`~~ | ❌ **2026-08-07 取消** | 控量收斂,併入通用圖 |
| **16d** | **結局專屬圖** | **`cut_ending.png`** | ⬜**待製作**（放檔即生效） | ★見下方「結局圖的構圖建議」。未產出前 outro 自動退回 `dialog_bg`,不會壞 |
| 19 | 教學提示圖（可選） | `tut_*.png` | 需接線 | 目前教學是純文字框，已可用；要圖示再加 |

> ★ **實際檔名是 `intro_01`~`intro_04`**(底線 + 補零),非本表原訂的 `intro1`~`intro4`。已依實際檔名接線,不需改名。

**四頁開場的視覺邏輯**:毀滅 → 廢墟 → 我 → 敵人(世界死了 → 現場 → 主角登場 → 對手登場)。不看文字也講得完。

### ★ 核爆頁(intro1)的三種畫法

**問題**:蘑菇雲是**垂直**的,但可視區是 **400×163 的扁橫幅**(當時規格為 136,更扁)。直接畫會被壓扁或切掉頭。三個解法,由推薦到普通:

| 畫法 | 做法 | 評價 |
|---|---|---|
| **A. 白光**(★推薦) | 不畫雲,畫**光**。爆炸瞬間畫面被白光吞沒,只剩城市天際線的**黑剪影**貼在下緣 | 最貼 1-bit、**最省**(大面積留白+剪影,沒有細節)、扁幅完全不是問題 |
| **B. 衝擊波** | 爆炸後一瞬,**橫向擴散的環**掃過地面 | 橫向動勢剛好吃滿 400 寬,但要處理漸層,1-bit 上較難 |
| **C. 只畫雲蓋** | 蘑菇頭壓在畫面上緣,地平線壓到 y≈120,雲柱只露一小截 | 觀眾會自動補完,但構圖最擠 |

**★ 只有這一張反轉**:其餘所有過場圖都是「白底 + 黑物件」,核爆這張走**黑底白光**(或大面積白)。
整個序列只有一張是反的,翻到它的瞬間衝擊力就出來了——這是 1-bit 才做得乾脆的手法。

**★ 同機位前後對比**(幾乎零成本、效果最大):
`intro1` 與 `intro2` 用**同一個機位、同一條天際線**。第 1 頁高塔還立著、被白光吞沒;第 2 頁同樣的天際線,高塔倒了。
翻頁的瞬間玩家自己看懂中間發生過什麼,**一個字都不用寫**。

**不牴觸「不必畫人」**:畫面只需要城市剪影,不需要任何人。核爆正是人類消失的那一刻——反而把「全機器人世界」的前提交代掉了。

### 文案(★ 2026-08-05 定稿並已接線,見 [intro_data.lua](Source/intro_data.lua))

一次只顯示一句。長度**量 px 不數字元**(上限 **384px**);每頁 2 句,**開場最後一頁為例外、可 3 句**。完整規則見 [GDD §1.5](GDD_正式版_組裝玩具任務.md)。

| 頁 | 文字 | 寬度 |
|---|---|---|
| intro_01 核爆 | `The fires burned for four days.` | 303px |
| | `Nothing that breathed survived.` | 297px |
| intro_02 廢墟 | `Then the war ended.` | 184px |
| | `Nobody told the machines.` | 242px |
| intro_03 核心 | `I woke up as a single toy core.` | 303px |
| | `Everything else, I had to build.` | 302px |
| intro_04 對峙 | `Weapons tear the weak toys apart.` | 328px |
| | `We toys need to help each other.` | 316px |
| | `Bolt on every part you find.` | 275px |

**敘事結構**:過去式無人稱(世界)→ 過去式第一人稱(我)→ 現在式 → 祈使句(推玩家進遊戲)。
最後一頁為「問題 → 回應 → 行動」三拍,這就是它可以用 3 句的理由。

### 切片關卡的過場圖分配與文字

**用圖欄已全部統一** —— 切片期間所有關卡與結局共用 `dialog_bg`，只有文字不同。

| 關 | 用圖 | 文字 |
|---|---|---|
| 1. SCRAP FIELD | `dialog_bg` | `Move the debris to the drop zone.` / `Your body is scrap too. Test it.` |
| 2. SCAVENGERS | `dialog_bg` | `They hunt the unarmed for parts.` / `Clear them out.` |
| 3. THE CARRIER | `dialog_bg` | `A carrier unit. No weapons at all.` / `Get it to the bunker. It cannot run.` |
| 4. THE SIGNAL | `dialog_bg` | `Something still transmits orders.` / `Follow the signal. Mind the cliffs.` |
| BOSS. OVERSEER | `dialog_bg` | `It is still waiting for an order.` / `One that will never come.` |
| **結局 ×3 頁** | **`cut_ending`** | 見 [outro_data.lua](Source/outro_data.lua) |

### ★ 結局圖 `cut_ending.png` 的構圖建議（2026-08-07 決定要做）

**規格**：400 × 163，1-bit。放進 `Source/images/` 重新編譯即生效，不必改程式。
結局三頁**共用這一張**（換文字不換圖，與關卡同慣例）。

**★ 最強的做法：跟 `intro_01`（核爆）同一個機位、同一條天際線。**

| | `intro_01` 開場 | `cut_ending` 結局 |
|---|---|---|
| 天空 | 蘑菇雲吞沒畫面 | **空的、乾淨** |
| 天際線 | 高塔還立著 | **塔倒了** |
| 情緒 | 毀滅 | 終於安靜 |

玩家在開場看過那個畫面，四十分鐘後看到同一個機位、雲散了、塔倒了——
**「結束了」這件事不用任何文字就講完了**。這正是 intro_01 → intro_02 用過的手法
（同機位前後對比），在結局重複使用會形成整部作品的收束。

成本也最低：**構圖你已經畫過一次了**，等於改天空與塔的狀態。

**三頁的文字都能吃這張圖**（`transmission stopped` / `guns went quiet` / `carriers came out to rebuild`）——
它們講的是同一個「之後」，不需要三種畫面。

> 若之後還有餘力，這張圖的「塔還立著、仍在發訊號」版本可以給**最終 BOSS 關**當過場，
> 形成 開場核爆 → BOSS 前塔還在 → 結局塔倒下 的三段呼應。但那是加分項，不做也完整。

**文案位置**:開場在 [intro_data.lua](Source/intro_data.lua)、**結局在 [outro_data.lua](Source/outro_data.lua)**、關卡在各 `Source/levels/*.json` 的 `scene.dialog`、教學在 [tutorial_data.lua](Source/tutorial_data.lua)。

**✅ 結局過場(outro)已實作**(S11,2026-08-03):全破後結算畫面出現 `CONTINUE` → 播 3 頁結局 → CREDITS(標題顯示 **THE END**)。
結局三頁已在 [outro_data.lua](Source/outro_data.lua) 定稿,分別用 `cut_core` / `cut_field` / `cut_survivor`。
**⬜ 這三張圖還沒畫（待製作優先序 2）**,程式會自動退回 `dialog_bg` 當暫代 —— 圖檔丟進 `Source/images/` 重新編譯即生效,**不必改任何程式**。
⚠️ 新規格是 **400 × 163**（不是 240），與 intro 四頁一致。

---

## 4. 新零件（C1）— ✅ **已完成並結案（2026-08-11）**

> ★★ **本切片的新零件就是雷射槍 `GUN2` 這一個，不再新增別的零件。**
> C1 到此結案；後續的零件構想全部歸到 [GDD §15.2](GDD_正式版_組裝玩具任務.md)（下一階段構想池），切片期間不做。

### ✅ 雷射槍 GUN2（本切片唯一的新功能零件）

| 素材 | 檔案 | 尺寸 | 狀態 |
|---|---|---|---|
| 零件本體 | `gun02.png` | **32 × 16**（2 格） | ✅ 完成並接線 |
| 操作面板 | 沿用 `gun_panel.png` | 32 × 32 | ✅ 沿用既有 |
| A 鍵圖 | 沿用 `canon_button-table-32-32.png` | 32×32 ×2 幀 | ✅ 沿用既有 |

**規格**（接線細節見 [parts_data.lua](Source/parts_data.lua) 的 `GUN2`）：
- **手動發射**：`operable = true` → 進焦點循環，焦點在它身上時**按 A 發射**（與 CANON 同一套）
- **會往前飛的貫穿光束**：不是 BOSS 那種瞬間打到畫面邊緣的長雷射，而是一段 **40px 的短線段**往前飛
- **速度快**：`laser_speed_mult = 150`（GUN 的砲彈是 40）
- **貫穿**：沿路每隻敵人各扣一次血，不會被擋下消失
- **CD 較長**：2.2 秒（GUN 是 1.0）
- 槍口量自圖的**右端** (31, 8)，寫成 `muzzle_x/muzzle_y` 資料欄位

★ 面板只用了 1 格（gun_panel）+ 1 格（A 鈕）＝ 64px，剛好對上 2 格寬的零件，
**不需要新畫面板圖**。日後若要專屬面板，畫 `gun02_panel.png`（64×32）覆蓋即可。

---

### 📐 零件素材組的通用規格（保留給正式版量產時參考）

一個零件的完整素材組（以佔 2 格寬、可 crank 操作為例）:

| # | 素材 | 檔名慣例 | 尺寸 | 接法 |
|---|------|----------|------|------|
| 20 | 零件本體 | `<part>.png` | 佔用格數 × 16（2格＝**32 × 16**） | ✅ 直接生效（寫進 `parts_data.lua` 的 `image`） |
| 21 | 子件（如砲管/臂，若有） | `<part>_barrel.png` 等 | 依設計（如砲管 64 × 8） | 需接線（每種子件都要程式配合） |
| 22 | 操作面板 | `<part>_panel.png` | 佔用格數 × 32（2格＝**64 × 32**） | ✅ 直接生效（`ui_panel`） |
| 23 | crank 旋鈕（若用 crank） | `<part>_control.png` | **32 × 32** | 需接線 |
| 24 | A 鍵圖（若用 A） | `<part>_button-table-32-32.png` | 32×32 ×2 幀（放/按） | 需接線 |

**參考現有**:`canon.png 64×8` + `canon_base.png 32×16` + `canon_panel.png 64×32` + `canon_control.png 32×32` + `canon_button-table-32-32.png`。

---

## 4.5 核心 CORE（2026-08-01 定案，見 [GDD §8.05](GDD_正式版_組裝玩具任務.md)）

**全案最便宜的美術項：3 張 16×16。**

| # | 素材 | 檔名 | 尺寸 | 接法 |
|---|------|------|------|------|
| 20a–c | 核心 三階（一、二、三） | `core-table-16-16.png` | **16 × 16 ×3 幀** | ⬜ **圖已完成**；系統尚未實作（見下） |

> ★ **2026-08-06**:使用者交的是**一張橫向 3 格的 sprite sheet**（48×16），比原訂的三張獨立檔更好。
> 原檔名 `core.png` 已改名為 `core-table-16-16.png` —— Playdate 的 imagetable **硬性要求** `-table-寬-高`，
> 不改名會被當成一張 48×16 的單圖讀進來（見 [HANDOFF §3-2](HANDOFF_切片開發現況.md)）。已編譯出 `core.pdt` 確認生效。
> 程式端用 `gfx.imagetable.new("images/core")` 讀取，第 1/2/3 格 = CORE1/CORE2/CORE3。

**CORE 系統實作狀態（2026-08-06，使用者選擇「先做安全部分」）**

| 項目 | 狀態 |
|---|---|
| [core_data.lua](Source/core_data.lua)（CORE1/2/3 的 `base_hp`/`weight_cap`/`frame`） | ✅ 已做 |
| `GameState.core_id` + 存檔讀寫（舊存檔自動回退 `CORE1`） | ✅ 已做 |
| HQ 畫出核心（組裝格正中央、**所有零件底層**） | ✅ 已做 |
| **HP ＝ 核心基礎 HP + 零件 HP 總和** | ✅ 已做（同時取代 `save_manager` 來源不明的硬編 `total_hp = 30`，改由 `core_data` 推導） |
| **`total_weight > 核心上限` 時擋住安裝** | ✅ 已做（擋下時 WEIGHT 那行反白閃爍，並播 cancel 音效） |
| 取得時機（幕別解鎖）、核心更換介面 | ⬜ **未做** — 需要進程系統，目前固定為 CORE1 |

> **實作重點**:`recalcMechStats()` 成為 mech_stats 的**唯一真相來源**——安裝/拆卸/清除重疊
> 全部改成呼叫它重算，不再各自 `+/-` 累加（舊做法有四處各自加減，會累積誤差，也帶不進核心基礎值）。
> 進 HQ 時也會重算一次，所以**舊存檔的 HP 會自動補上核心基礎值**。

**★ CORE1 上限 16 對現有 8 零件的實際影響**（已實測零件重量）

| 組合 | 總重 | CORE1(16) |
|---|---|---|
| `CLAW(8) + CANON1/2(10)` | 18 | ❌ 擋下 |
| `CANON1(10) + CANON2(10)` | 20 | ❌ 擋下 |
| `CANON1/2(10) + FEET(7)` | 17 | ❌ 擋下 |
| 其餘所有兩件組合 | ≤16 | ✅ 可裝 |

也就是說 **CORE1 階段等於「重砲不能配重腿/重爪」**——這正是 GDD 想要的取捨。
如果實際玩起來覺得太緊，調整點是 `parts_data.lua` 的 `weight` 或 `core_data.lua` 的 `weight_cap`。

**構圖須知**

- 核心**不佔組裝格、關卡中完全不顯示**，只出現在組裝畫面與商店。
- 顯示位置＝**組裝格正中央、畫在所有零件的底層**。裝滿零件時會被蓋掉大半（這是刻意的——
  「拼裝把核心包起來」），所以**造型辨識度要放在輪廓與中心，不要靠邊緣細節**。
- 空 build 時畫面上只有這顆核心 —— **要和 `intro_03.png` 裡那顆核心特寫是同一個東西**。
  ⚠️ **順序已經反過來了**:`intro_03` 已經畫好了，所以 `core1~3` 要**照 intro_03 那顆核心的造型畫**，
  而不是原訂的「先定 core 再畫 intro」。
- 三階要能一眼看出「同一顆東西的三個世代」：建議**外形骨架不變，只加裝甲/加發光紋/加接點**。
  一階＝撿來的、有破損；三階＝完整、對稱、有權威感。

---

## 5. 新敵人（C3）— ✅ **已完成並結案（2026-08-12）**

> ★★ **本切片的新敵人就是 WALKER 這一種，不再新增。**
> C3 到此結案；後續的敵人構想全部歸到 [GDD §15.4](GDD_正式版_組裝玩具任務.md)（下一階段構想池）。

### ✅ WALKER（本切片唯一的新敵人）

| 素材 | 檔案 | 尺寸 | 狀態 |
|---|---|---|---|
| 敵人本體 | `enemy04-table-40-32.png` | **40 × 32 × 3 幀** | ✅ 完成並接線 |

**行為**（詳見 [enemy_data.lua](Source/enemy_data.lua) 的 `WALKER_ENEMY`）：
- 基本同 BASIC（前後己邏、遇斜坡換方向），但**移動快很多**（`move_speed` 55 vs BASIC 20）
- **走一段停一段**（`move_duration` 1.6s / `pause_duration` 1.4s）
- ★ **只在停下時開火**（`fire_only_when_stopped`）——移動中不攻擊
- 動畫：第 1 格＝站立（停下時）、第 2~3 格＝走路循環
  ★ **不要設 `anim_fps`** —— 那是無條件循環，會讓它停著也在走
- ⚠️ **原圖面向右**，但本作敵人一律面向左（玩家從左側來）
  → 用新增的 `flip_x = true` 水平鏡射；`bullet_offset` 也是鏡射後的座標
  （槍口量自原圖 (34, 7) → 鏡射後 x = 40 − 34 = 6）

---

## 6. 關卡場景（C5 — 切片 3~4 關）

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 備註 |
|---|------|----------|------|------|------|
| 27 | **天空/雲（最遠層）** | `bg_sky.png` | **400 × 160** | ✅**已完成並接線**（2026-08-05 新增系統） | 見下方「天空層」 |
| 28 | 背景建物 | ~~`bg_<theme>1..4.png`，100~150 寬 × 100~250 高~~ → **`bg_building1~7.png`（7 張）** | 1~3：**128×128**<br>4~6：**64×64**<br>7：**128×32** | ✅**已完成**（2026-08-11 重繪並擴充） | 直接生效（關卡 JSON 的 `backgrounds.image`）。`y` ＝**圖左上角的螢幕 y**（背景不吃垂直攝影機位移）→ **改圖高度會讓底邊跑掉**，換圖時關卡座標要一起重算<br>★ 編輯器已能**連結 `Source/images` 資料夾自動抓圖與真實尺寸**（見 [HANDOFF §6](HANDOFF_切片開發現況.md)），新增 `bg_building8`… 不必再改編輯器 |
| 29 | 障礙物 | `obstacle_<theme>.png` | **32 × 32** | ✅ 直接生效（`obstacles.image`） |
| 30 | **運送箱 / 運送目標** | `crate.png` / `target-table-32-32.png` | 24×19 / **32×32 ×4 幀** | ✅**已完成並接線**（2026-08-09） | 取代舊的 `stone.png`/`target_box.png`。目標會**浮空 5px、上下飄**，箱子放上去後**往左上飛走消失**（舊的擴散圓環特效已移除）<br>⚠️ 目標是 imagetable，檔名**必須**含 `-table-32-32` |
### ★ 天空層（2026-08-05 新增，`scene.sky`）

與 `backgrounds` 是**不同的系統**:天空會**橫向平鋪**（backgrounds 只畫一次）、視差**更慢**、**不需要 x/y 座標**。
關卡 JSON 每個 scene 加一行即可，已在 M008 三個場景接上:

```json
"sky": "images/bg_sky",
"sky_parallax": 0.1,   // 選填，預設 0.1，越小越遠
"sky_y": 0             // 選填，預設 0
```

| 項目 | 規格 |
|---|---|
| 尺寸 | **400 × 160**（畫在 y=0） |
| 可視範圍 | **y = 0 ~ 156**（地面線＝`ground_y` 220 − UI 64）。以下會被地形黑色填滿蓋掉 |
| 平鋪 | 橫向重複，**左右邊緣必須無縫** |
| 繪製順序 | `clear(白)` → **天空** → backgrounds → 地形 → 敵人 → 機甲 → foregrounds → UI |

**★ 唯一硬性規則:戰鬥帶（y≈110~156）必須留白。**
敵人是**黑色實心剪影**,天空在那一帶只要有黑塊,敵人就會消失。
上半部（y=0~100）可以放心用實心與網點——現行 `bg_sky.png` 上半 45~68% 黑、戰鬥帶 1.2% 黑,實測沒問題。

**兩個待實測（我無法自動驗證,要你打一場）**
1. **跳躍最高點**:`JUMP_VELOCITY=-10`/`GRAVITY=0.5` → 跳高 **100px**,機甲會升到 y≈24~56,正好在雲最濃的地方。機甲是白底黑線,白色主體在黑雲上更顯眼,但**黑色輪廓會消失**。子彈若是黑色小點,飛到上半可能看不見。
2. **平鋪接縫**:現行 `bg_sky.png` 左右邊緣有 **27/160 列對不上**,平鋪處會有垂直接痕。但視差 0.1 下走完 800px 的場景天空只位移 80px,接縫可能不會進到畫面——先實測再決定要不要修圖。

**地面（不需美術素材；程式繪製）**

- 地面**維持純黑填充**。曾試過 8×8 `setPattern` 貼圖（含多樣式隨機交錯），實測效果不佳已移除——主因是 Playdate 的填充樣式**對齊螢幕座標**，鏡頭捲動時花紋不會跟著地面走，看起來會「滑動」。
- 改為在地形線下方加一條**表面層**（白色帶狀 + 上下黑線，預設厚度 **6px**），斜坡自動貼合。厚度可調:[entity_controller.lua](Source/entity_controller.lua) 的 `SURFACE_T`（或設 `controller.surface_thickness`）。
- 若之後想把表面層換成**圖片**（草皮/金屬紋）:需要 **64 × 6** 的橫向貼圖，斜坡處以旋轉貼合——要做再跟我說。

★**控量關鍵**:切片只做**1 個世界觀主題**的背景組（3~4 張建物），量產階段再依主題分組共用——這是把 25 關場景美術壓下來的主要手段。

---

## 6.5 現有素材與排版修正（2026-07-30 追加）

### A. 四個 UI 畫面

前三個畫面目前**完全是程式繪製、沒有底圖**。建議沿用 `hq_bg.png` / `shop_bg.png` 的成功模式:**你畫 400×240 底圖(含框線與標題字樣),我把內容對齊你的框** —— 這個分工在組裝介面與商店都很順。

| # | 素材 | 檔名建議 | 尺寸 | 接法 | 現況 |
|---|------|----------|------|------|------|
| 31 | 遊戲標題畫面 | `cover.png` | **400 × 240**<br>★**透明底** | ✅**已完成並接線**（2026-08-11） | 2026-08-11 重繪成**只剩標題字、其餘透明**（不透明範圍 y=16~142）。<br>★ 下層由程式生成**零件隨機組合的 4 排橫向捲動**：1/3 排向右、2/4 排向左,13 種零件圖隨機抽取。參數全在 [state_menu.lua](Source/state_menu.lua) 開頭<br>⚠️ 零件圖有 **60~83% 是黑的**,四排鋪滿會吃掉黑色標題字 → 疊了一層 50% Bayer 白點減淡（`BG_DIM = 0.5`）。要零件清晰就設 0,但標題會糊<br>選項文字仍為程式繪製 |
| 32 | 選擇存檔畫面 底圖 | `save_bg.png` | **400 × 240** | ✅**已完成並接線**（2026-08-11） | ⚠️ 這張是**滿版細點陣場景圖**，不是 `hq_bg` 那種留白的框線底圖 → 黑字疊上去會消失。<br>程式改成**白卡片疊在底圖上**（存檔槽列未選中＝白底、選中＝黑底白字;標題／BACK／提示鋪白底），版面座標沒動<br>★ 之後若要改成留白式底圖（本節開頭建議的分工），把那些白底拿掉即可 |
| 33 | 選擇任務畫面 底圖 | `mission_select_bg.png` | **400 × 240** | 🟡**暫用 `save_bg.png`**（2026-08-11） | 專屬底圖尚未製作，先共用存檔畫面那張。<br>任務列**未選中改成先填白再描框**、標題／進度／捲動提示鋪白底;版面座標沒動<br>需求不變:任務列(4 列可視)+ 標題 + 進度/CLEAR 位 |
| 34 | 結算畫面 底圖 | `result_bg.png`（或勝/敗各一張 `result_win.png` / `result_lose.png`） | **400 × 240** | 需接線 | 純程式畫。**2026-08-11 版面已改定**，畫底圖時照這個留白：<br>・`y≈60` 標題一行（成功＝`MISSION COMPLETE`;失敗＝`MISSION FAILED` + `y≈90` 失敗原因）<br>・`y=100/122/144` 資源三列 —— **x 118~295** 之間要留白（標籤 118 起、數字右對齊到 248、`+N` 在 260 起）<br>・`y≈180` 選項列（NEXT / RETRY / MISSION SELECT），置中<br>★ 資源列是**三段式動畫**（現有數字 → 閃爍 +N → 最終數字），那條帶狀區域不要放深色圖案 |

> 畫底圖時**框內留白**即可,文字與數值一律由程式填(才能顯示動態內容)。畫好後跟我說,我用像素量測對齊(同 hq_bg 的流程)。

### B. 現有素材尺寸/造型修正

| # | 素材 | 現況尺寸 | 修正方向 | 連動注意 |
|---|------|----------|----------|----------|
| 35 | CANON1 砲管 | ~~64 × 8~~ → **40 × 16** | ✅**已完成**（2026-08-08） | 砲口發射點與各處預覽都是讀圖寬算出來的,自動對位 |
| 36 | CANON2 砲管 | ~~64 × 12~~ → **40 × 16** | ✅**已完成**（2026-08-08） | 同上 |
| 37 | FEET | ~~`feet.png` 48 × 32~~ → **48 × 20** | ✅**已完成**（2026-08-07，比建議的 24 更矮） | 靜圖 `feet.png` 48×20、動畫 `feet_walk-table-48-20.png` **4 幀**（原 3 幀）。踩地高度與幀數皆自動跟上;舊的 `feet_walk-table-48-32.png` **已刪除**（兩個 `feet_walk-table-*` 並存會讓 pdc 產生衝突的 `feet_walk.pdt`） |
| 38 | 爆炸特效 | `mine_explode-table-50-50.png`（3 幀） | ✅**已完成**（2026-08-07 重繪，維持 3 幀 50×50） | 幀數不限（程式讀 `getLength()`）,改名的數字要對應單格尺寸<br>此表**地雷/敵人死亡/BOSS 死亡共用** |
| 39 | 飛行敵人 DRONE | ~~借用 `enemy1.png`~~ → **`enemy_drone-table-32-32.png`（6 幀）** | ✅**已完成並接線**（2026-08-09） | 旋翼循環播放（`anim_fps = 12`）。子彈從**下方槍口**射出，座標量自 sprite：槍管 x=13~18 / y=20~23 → 出口 (16, 24)，寫在 `enemy_data` 的 `bullet_offset_x/y`<br>★ 敵人載圖已改成**一律先試 imagetable**，之後任何敵人要動畫只要放 `-table-寬-高` 的圖 + 設 `anim_fps` |
| 40 | **揮劍敵人 SWORD_ENEMY** | `enemy2.png` ~~32 × 40~~ → **32 × 32**<br>`enemy2_sword.png` 48 × 16（未變） | ✅**已完成並接線**（2026-08-10） | 身體矮 8px,命中框讀圖自動跟上。<br>★ 劍圖握把圓環中心由 (8.0, 8.0) → **(7.0, 8.0)**,`sword_image_pivot_offset_x` **−16 → −17**<br>★ 同時新增 `sword_length` 資料欄位(**41**,量自圓環中心 x=7 → 劍尖 x=48),**繪製端與命中判定端改成共用**——原本判定端寫死 30,比劍身短 11px |
| 41 | **地雷 MINE** | ~~`mine.png` 單張 32×16~~ → **`mine-table-32-16.png`（3 格）** | ✅**已完成並接線**（2026-08-10） | **第 1 格＝本體**（畫布下緣 y=9~15，踩地面）一直畫;**第 2/3 格＝警示燈**（畫布上緣 y=2~9），觸發後交替閃爍。<br>★ 三格共用同一張 32×16 畫布 → **同座標疊畫即對位**，不需任何偏移<br>★ 閃爍參數在 `enemy_data` 的 `warn_frame_a/b` 與 `warn_blink_speed`(10)<br>★ 順手移除觸發時的舊「白框閃爍」與爆炸時的 `EXP:n/n` 除錯字<br>★ 舊的 `mine.png` **已刪除**（2026-08-10），pdx 裡只剩 `mine.pdt` |
| 42 | **CLAW 開合開關鈕** | ~~沿用佔位的 `claw_control_v-table-32-32`（3 格）~~ → **`claw-button-table-32-32.png`（2 格）** | ✅**已完成並接線**（2026-08-10） | 面板左格。**第 1 格＝爪子打開、第 2 格＝夾起**<br>⚠️ **順序與舊佔位圖相反**（舊圖 1=關 / 2=開）——換圖時這一行沒跟著改的話，按鈕會顯示相反的狀態<br>★ 舊的 `claw_control_v-table-32-32.png` **已刪除**（2026-08-10），fallback 分支一併移除 |
| 43 | **WHEEL2** | `wheel2.png` ~~48 × 16~~ → **48 × 20** | ✅**已完成並接線**（2026-08-10） | 比格子（16px）高 4px，所以改成**與 FEET 同一套原則**:`align_image_top = true`（上緣對齊格子上緣，多出的 4px 往下超出格子）<br>★ 只要改 `parts_data` 這一個布林 —— 機體上、HQ 組裝格、HQ 預覽、商店共 7 個繪製點**全部都讀同一個欄位**<br>★ WHEEL1（`wheel.png` 48×16）維持原樣不受影響 |

> **★ CANON 的圖層與對齊規則（2026-08-08 統一）**：**砲座先畫、砲管疊上**，兩者**左緣對齊、底部對齊**。
> 四個繪製點都遵守：機體上（`entity_mech_render` 的 `drawPart` / `drawActivePart`）、HQ 組裝格、HQ 預覽框、商店。
> 舊版的 HQ 組裝格把砲座畫在最後、商店把砲管對齊砲座中心——那是為 **64×8 細長砲管**設計的，
> 砲管改成 **40×16** 後就會「砲座蓋住砲管」和「砲管往右偏半個底座」。已全部修正。

---

## 7. 建議製作順序（先解鎖驗證、再美化）

> 2026-08-05 更新:BOSS、場景物件、開場四頁、天空層**均已完成**，已從本順序移除。以下是剩下的。

1. **核心 CORE 三張**（#20a–c，16×16）—— 全案最便宜，**照 `intro_03` 已畫好的那顆核心來畫**
2. ~~結局過場三張~~ **已取消**（2026-08-07 控量）。若行有餘力，只補**結局專屬 1 張**，
   理由見 §3 「結局是否該有專屬圖」
3. ~~**小修正組（便宜、改善手感辨識）**:CANON 砲管改短（#35–36）、FEET 改矮（#37）、DRONE 專屬圖（#39）、爆炸特效（#38）~~ ✅ **2026-08-10 全數完成**（另加碼 #40 揮劍敵人）
4. ~~**UI 底圖四張（#31–34）**~~ — `cover`(#31) 與存檔(#32) ✅ **2026-08-11 完成**；選關(#33) 暫用 `save_bg`；**剩結算(#34)**
5. **REACH 目標旗（#13）** — 最後一個還是程式畫方塊的場景物件
6. ~~**新零件（#20–24）+ 新敵人（#25–27）**~~ ✅ **兩項皆結案**（GUN2 見 §4、WALKER 見 §5）
7. ~~**場景背景主題組（#28）**~~ ✅ **2026-08-11 完成**（`bg_building1~7`）。剩 **障礙物 #29** 一項

## 8. 數量小結（切片）

| 分區 | 原估 | ✅ 已完成 | ⬜ 剩餘 |
|------|------|------|------|
| BOSS | 4~5 | **4**（合併成 1 張 imagetable） | 0~1（殘骸，可選） |
| 場景物件 | 6~8 | **5**（砲台×2、面板、NPC、傳送箭頭、bunker） | 0~1（僅剩可選的懸崖邊緣；目標旗已取消） |
| 過場圖 | ~~7~~ → **5** | **5**（intro_01~04 + 通用 `dialog_bg`） | **0** |
| **核心 CORE** | 3 | **3**（`core-table-32-16.png` 一張 3 格） | **0** |
| 天空層 | —（新增） | **1**（`bg_sky`） | 0 |
| 新零件（C1） | 2~5 | **1**（`gun02.png`;面板與 A 鈕沿用既有） | **0**（★ 已結案，不再新增） |
| 新敵人（C3） | 1~3 | **1**（`enemy04-table-40-32`） | **0**（★ 已結案） |
| 場景背景（#28） | 4~5 | **7**（`bg_building1~7`，2026-08-11 重繪並擴充） | **0** |
| 障礙物（#29） | 1 | 0 | **1** |
| **UI 畫面（#31–34）** | 4~5 | **2**（`cover` 透明底＋零件捲動、`save_bg`） | **1~2**（結算 #34;選關 #33 已暫用 `save_bg`） |
| **現有素材修正（#35–39）** | 5~7 | **5**（CANON×2 改短、FEET 改矮、爆炸重繪、DRONE 專屬圖） | **0** |
| **加碼重繪（#40–43，不在原估內）** | — | **5**（enemy2＋劍、地雷 3 格、CLAW 鈕、WHEEL2） | — |
| **合計** | 約 32~37 | **約 40** | **0**（必做項目已結案） |

> 對照 [Schedule](Schedule_正式版_組裝玩具任務.md):切片美術估 15~24 人日。**已完成約 38 張,剩 3~7 張**（逐項見 §0.5）。
> ★ 新零件只花了 **1 張圖**（面板與 A 鈕都沿用既有）—— 這是 QA2 值得記下的一筆:
> **「一個新功能零件 = 5 張圖」的原估過於保守,沿用既有 UI 元件就能壓到 1 張。**
> 已完成數已**超過原估總量**——因為途中有 5 張加碼重繪、背景也從 4 張變 7 張。
> ★ 這正是 QA2 要量的東西:**原估會低估,重繪與擴充才是真實成本**。
> ★ 實際畫完的張數與時間請記錄下來——那就是 **QA2 單位成本量測**的原始資料,決定 25 關/5 王是否下修。
> 目前為止已完成的部分「一張平均花多久」是最重要的一個數字,還沒有紀錄的話從下一張開始記。
